---
phase: 02-deterministic-merged-read-cache
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/protocol/narinfo.ts
  - src/nix/merged_cache.ts
  - src/nix/http_handler.ts
  - src/runtime/daemon.ts
  - tests/protocol/narinfo_test.ts
  - tests/integration/merged_cache_test.ts
autonomous: true
requirements: [READ-05, READ-06]
must_haves:
  truths:
    - "One request-captured ordered snapshot is probed across every available layer under one shared bounded request budget (D-05)."
    - "Only records identical in every supported non-Sig semantic field contribute signatures, and every valid Sig occurrence including byte-identical duplicates is appended in stable layer/occurrence order (D-06, D-07)."
    - "Any semantic disagreement returns the complete highest-priority record unchanged and emits one redacted typed diagnostic per losing record without failing the read (D-08 through D-11)."
    - "A subsequent NAR request is boundedly pinned to the exact publication snapshot whose Narinfo won, preventing cross-publisher metadata/blob mixing (D-12)."
  artifacts:
    - path: src/protocol/narinfo.ts
      provides: "Lossless complete non-signature semantic projection and signature occurrence append"
    - path: src/nix/merged_cache.ts
      provides: "Merged Narinfo resolver, typed conflicts, and bounded winner-route registry"
      exports: [MergedCacheDiagnostic, DiagnosticSink, WinnerRouteRegistry, resolveMergedNarInfo]
    - path: src/nix/http_handler.ts
      provides: "Snapshot-bound merged Narinfo GET/HEAD and winner-pinned NAR serving"
    - path: tests/integration/merged_cache_test.ts
      provides: "Agreement, conflict, budget, diagnostics, HEAD, and NAR provenance regressions"
  key_links:
    - from: src/nix/http_handler.ts
      to: src/nix/merged_cache.ts
      via: "one captured selection and budget feed all Narinfo probes"
      pattern: "resolveMergedNarInfo"
    - from: src/nix/merged_cache.ts
      to: src/protocol/narinfo.ts
      via: "complete semantic comparison gates raw Sig occurrence append"
      pattern: "semantic|signatures"
    - from: src/nix/http_handler.ts
      to: src/hashtree/reader.ts
      via: "NAR route lookup supplies exact winner publication to resolverFor"
      pattern: "winner|route"
---

## Phase Goal

**As a** Nix cache operator, **I want to** expose several trusted publishers as one priority-ordered cache, **so that** overlap is deterministic and observable without modifying Nix.

<objective>
Implement protocol-correct duplicate Narinfo merging, typed conflict observability, and winner-pinned NAR routing over the ordered snapshot (D-05 through D-12).

Purpose: Preserve all compatible trust evidence while preventing metadata and bytes from different publishers from being mixed.
Output: Lossless semantic comparison, stable signature concatenation, typed conflicts, bounded route pins, HTTP integration, and tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deterministic-merged-read-cache/02-CONTEXT.md
@.planning/phases/02-deterministic-merged-read-cache/02-RESEARCH.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-04-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-09-SUMMARY.md
@.planning/phases/02-deterministic-merged-read-cache/02-01-SUMMARY.md

<interfaces>
`parseNarInfo(text)` currently retains required scalar values, signatures with `rawLine`, fingerprint, and `rawText`; `createNixHttpHandler` captures selection before awaits and constructs a `RequestBudget`; `PathResolver.resolve(root,path,method,budget,signal)` returns a verified path result. Extend these contracts without rebuilding the winner's scalar lines.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Merge one agreeing pair through stock-Nix Narinfo GET and HEAD</name>
  <files>src/protocol/narinfo.ts, src/nix/merged_cache.ts, src/nix/http_handler.ts, tests/protocol/narinfo_test.ts, tests/integration/merged_cache_test.ts</files>
  <read_first>NIP.md, src/protocol/narinfo.ts, src/nix/http_handler.ts, src/hashtree/reader.ts, tests/protocol/narinfo_test.ts, tests/integration/http_cache_test.ts</read_first>
  <behavior>
    - "Two records equal across all required fields plus optional Deriver/System/CA presence and values yield the highest-priority raw scalar body with all valid Sig occurrences appended in layer and occurrence order per D-06/D-07."
    - "Byte-identical duplicate Sig lines remain present; no nixSigKey-based filtering, normalization, rewriting, or deduplication occurs per D-07 and NIP.md."
    - "Different raw scalar ordering or numeric spelling that parses to the same complete semantics does not prevent agreement; winner scalar encoding/order remains unchanged per D-06."
    - "Narinfo HEAD performs bounded metadata resolution and reports the exact merged byte length with no response body."
  </behavior>
  <action>Extend the Narinfo parser with a lossless supported-field representation and canonical non-signature semantic projection covering required fields and optional `Deriver`, `System`, and `CA`, including presence versus absence. Preserve every syntactically valid `Sig` occurrence and raw line. Add `src/nix/merged_cache.ts` with a sequential priority-ordered resolver that consumes one captured `MergedSelectionSnapshot`, one shared `RequestBudget`, and one abort signal per D-05. For compatible records, retain the winner's entire original non-signature layout and existing signature occurrences, then append every lower-priority signature occurrence without filtering or deduplication per D-06/D-07; the research's earlier deduplication note is superseded by current CONTEXT.md and normative NIP.md. Integrate Narinfo GET and HEAD into the HTTP handler, ensuring HEAD resolves bounded metadata to compute exact merged `Content-Length` but returns no body. Keep metadata bounded by `decodedMetadataBytes` and cancel failed probes.</action>
  <verify>
    <automated>deno test tests/protocol/narinfo_test.ts &amp;&amp; deno test --allow-read --allow-write tests/integration/merged_cache_test.ts --filter "agreement|duplicate signature|HEAD"</automated>
  </verify>
  <acceptance_criteria>Focused tests cover every recognized optional field, absence differences, alternate scalar ordering, duplicate identical signatures, publisher/occurrence ordering, one shared budget, and exact merged HEAD length.</acceptance_criteria>
  <done>Stock Nix receives a protocol-correct compatible signature concatenation without losing or rewriting any occurrence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve deterministic conflicts and pin NARs to the winner snapshot</name>
  <files>src/nix/merged_cache.ts, src/nix/http_handler.ts, src/runtime/daemon.ts, tests/integration/merged_cache_test.ts</files>
  <read_first>src/nix/merged_cache.ts, src/nix/http_handler.ts, src/runtime/daemon.ts, src/nostr/selection.ts, src/hashtree/reader.ts, tests/integration/http_cache_test.ts</read_first>
  <behavior>
    - "A difference in any required or optional non-Sig field returns the highest-priority complete raw record unchanged and emits one diagnostic for that loser per request per D-08/D-10."
    - "The diagnostic contains a stable code, store-path hash, winner/loser identity and event ids, and sorted differing field names, but no record text, URLs, hashes, signatures, or NAR bytes per D-09/D-10."
    - "Conflict diagnostics are nonfatal; typed absence, integrity, budget, and transport failures preserve existing 404/502/504 mappings per D-11."
    - "After Narinfo response, its normalized URL/path resolves through a count-and-TTL-bounded registry to the exact immutable winner snapshot; deterministic eviction and expiry never redirect an existing pin to a loser per D-12."
  </behavior>
  <action>Define a discriminated `MergedCacheDiagnostic` union and injected `DiagnosticSink`; emit exactly one conflict event for each semantically disagreeing loser after comparing against the priority winner, with only stable identity/event references and differing field names per D-08 through D-11. Implement a `WinnerRouteRegistry` whose constructor requires finite maximum entries and TTL, whose keys are normalized relative NAR paths accepted by the handler, and whose values are immutable winner publications. Register only after a Narinfo has been successfully selected; for NAR GET/HEAD, consult a live pin first and pass that exact publication to `resolverFor` per D-12. On missing/expired pins, perform a bounded ordered direct lookup only if it resolves the requested NAR within one captured snapshot; never replace a valid pin based on a later selection. Wire required registry bounds and the typed sink in production without unstructured record logging. Preserve PUT 405 and all existing route/error semantics.</action>
  <verify>
    <automated>deno test --allow-read --allow-write tests/integration/merged_cache_test.ts</automated>
  </verify>
  <acceptance_criteria>Every supported field disagreement is tested; the winner body is byte-identical, diagnostics are one-per-loser and secret-safe, and winner NAR bytes remain pinned across a reactive selection update with deterministic registry bounds.</acceptance_criteria>
  <done>Conflicts stay visible and nonfatal while Narinfo metadata and NAR bytes retain exact winner provenance.</done>
</task>

</tasks>

## Artifacts this plan produces

- `src/protocol/narinfo.ts`: complete lossless semantics and raw signature occurrence support
- `src/nix/merged_cache.ts`: merged resolution, diagnostics, and bounded winner routes
- `src/nix/http_handler.ts`: merged Narinfo and winner-pinned NAR HTTP behavior
- `tests/protocol/narinfo_test.ts`: optional-field and signature preservation tests
- `tests/integration/merged_cache_test.ts`: full agreement/conflict/provenance integration matrix

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated publisher metadata -> merged response | Hostile but signed records may conflict, amplify work, or smuggle untrusted contents into diagnostics. |
| Narinfo response -> later NAR request | A path-only NAR request must retain the publication provenance selected previously. |
| multiple resolvers -> request budget | Per-layer probing must not multiply transfer, traversal, attempt, or decoded-byte ceilings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02-01 | Tampering | semantic comparison/signature merge | high | mitigate | Compare every supported non-Sig semantic field and concatenate all raw valid Sig occurrences only on full agreement. |
| T-02-02-02 | Tampering | NAR winner provenance | high | mitigate | Count/TTL-bounded immutable route pins and update-race integration tests bind NAR resolution to the Narinfo winner. |
| T-02-02-03 | Information disclosure | conflict diagnostics | high | mitigate | Typed allow-listed fields only; tests assert hostile record contents and signatures are absent. |
| T-02-02-04 | Denial of service | multi-layer probing/route registry | high | mitigate | One request budget across sequential probes plus required registry cardinality/TTL and deterministic eviction. |
| T-02-02-SC | Tampering | dependency graph | high | mitigate | No package install or lockfile mutation; use project-owned pure merge logic and existing parsers. |
</threat_model>

<verification>
Run `deno task fmt`, `deno task lint`, `deno task check`, `deno test tests/protocol/narinfo_test.ts`, the complete merged-cache integration suite, the existing HTTP cache suite, and `deno task test:nix-e2e`.
</verification>

<success_criteria>
- Compatible records concatenate every signature occurrence in stable priority/record order.
- Every non-signature disagreement preserves the complete winner and emits bounded typed diagnostics.
- Narinfo HEAD length is exact and all probes share one request budget.
- NAR routing cannot mix a winner's metadata with another publication's bytes.
</success_criteria>

## Source Coverage Audit

| Source | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe overlap handling for one predictable cache | 02-02 | COVERED | Implements compatible merge and conflict winner. |
| REQ | READ-05 | Union signatures only on complete semantic agreement | 02-02 | COVERED | Task 1. |
| REQ | READ-06 | Priority winner and structured conflict warning | 02-02 | COVERED | Task 2. |
| RESEARCH | — | Complete optional-field semantics, HEAD body resolution, shared budget | 02-02 | COVERED | Task 1. |
| RESEARCH | — | Bounded winner route registry and redacted diagnostic | 02-02 | COVERED | Task 2; constructor-required bounds resolve A2/A3. |
| CONTEXT | D-05 | Probe all layers from one captured snapshot | 02-02 | COVERED | Task 1. |
| CONTEXT | D-06/D-07 | Winner scalar preservation and all Sig occurrences | 02-02 | COVERED | Task 1. |
| CONTEXT | D-08/D-09/D-10/D-11 | Winner conflict behavior and typed nonfatal diagnostics | 02-02 | COVERED | Task 2. |
| CONTEXT | D-12 | Winner-pinned NAR retrieval | 02-02 | COVERED | Task 2. |
</context>

<output>
Create `.planning/phases/02-deterministic-merged-read-cache/02-02-SUMMARY.md` when done.
</output>
