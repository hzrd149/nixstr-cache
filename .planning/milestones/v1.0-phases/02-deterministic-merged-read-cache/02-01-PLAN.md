---
phase: 02-deterministic-merged-read-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/config.ts
  - main.ts
  - src/nostr/selection.ts
  - src/runtime/daemon.ts
  - tests/integration/operator_config_test.ts
  - tests/integration/publication_selection_test.ts
autonomous: true
requirements: [PROT-01]
must_haves:
  truths:
    - "The operator configures one bounded ordered list of exact default and named cache identities; that order alone defines publisher priority (D-01, D-02)."
    - "Each configured identity selects, expires, and withdraws independently while preserving its own durable anti-rollback state (D-03, D-04)."
    - "The reactive read surface exposes an immutable ordered snapshot of currently available identities, unaffected by relay arrival order or cross-identity timestamps (D-01, D-03)."
    - "Production relay filters include kinds 17091 and 37091 for the configured authors, while exact identity authorization still precedes durable admission."
  artifacts:
    - path: src/config/config.ts
      provides: "Canonical ordered CacheIdentity configuration and bounded identity cardinality"
      exports: [ValidatedConfig, parseConfig]
    - path: src/nostr/selection.ts
      provides: "Applesauce custom model for independently selected ordered publications"
      exports: [MergedSelectionSnapshot, CacheSelectionModel, startPublicationSelection]
    - path: src/runtime/daemon.ts
      provides: "Production subscription and merged selection composition"
    - path: tests/integration/publication_selection_test.ts
      provides: "Ordering, freshness, expiry, withdrawal, immutability, and admission regressions"
  key_links:
    - from: src/config/config.ts
      to: src/nostr/selection.ts
      via: "validated identities array is passed without set-based priority loss"
      pattern: "identities: config\\.identities"
    - from: src/nostr/selection.ts
      to: src/persistence/state_repository.ts
      via: "per-identity repository acceptance occurs before EventStore admission"
      pattern: "repository\\.accept"
    - from: src/runtime/daemon.ts
      to: applesauce-relay
      via: "subscription filters both publication kinds for derived authors"
      pattern: "17091.*37091|37091.*17091"
---

## Phase Goal

**As a** Nix cache operator, **I want to** expose several trusted publishers as one priority-ordered cache, **so that** overlap is deterministic and observable without modifying Nix.

<objective>
Generalize the Phase 1 scalar selection into a production-wired ordered reactive selection for exact default and named cache identities (D-01 through D-04).

Purpose: Establish the priority and snapshot contract every merged metadata and NAR request consumes.
Output: Canonical ordered configuration, independent per-identity selection, production relay filters, and focused integration coverage.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deterministic-merged-read-cache/02-CONTEXT.md
@.planning/phases/02-deterministic-merged-read-cache/02-RESEARCH.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-10-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-11-SUMMARY.md

<interfaces>
Existing `ValidatedConfig.identities` is `readonly string[]`; `startPublicationSelection` accepts `publisherPubkeys` and `identities`; `PublicationSelector.current()` currently returns one `SelectedPublication`. Replace the scalar view with an immutable ordered snapshot while retaining validate -> authorize -> repository.accept -> EventStore.add.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one ordered default-plus-named selection through production configuration</name>
  <files>src/config/config.ts, main.ts, src/nostr/selection.ts, src/runtime/daemon.ts, tests/integration/operator_config_test.ts, tests/integration/publication_selection_test.ts</files>
  <read_first>src/config/config.ts, main.ts, src/nostr/selection.ts, src/runtime/daemon.ts, src/protocol/publication.ts, src/persistence/state_repository.ts, tests/integration/operator_config_test.ts, tests/integration/publication_selection_test.ts</read_first>
  <behavior>
    - "A raw ordered list containing `17091:<pubkey>:` followed by `37091:<pubkey>:<exact-d>` validates and reaches the production selector in that exact order per D-01/D-02."
    - "A valid publication for each identity yields a frozen two-layer snapshot in configuration order even when the lower-priority event is newer or arrives first per D-01/D-03."
    - "The named identity retains its exact raw d value; duplicate identities, malformed kinds/pubkeys/default suffixes/named d values, and an over-limit list fail before side effects per D-02."
  </behavior>
  <action>Add one explicit ordered cache-identity environment/config field and parse each entry with a shared canonical `CacheIdentity` parser accepting only exact kind 17091 default identities and kind 37091 named identities per D-02. Preserve exact nonempty named `d`, reject duplicate raw identities, derive a deduplicated authors list only as secondary relay-filter data, and enforce a compiled maximum identity count so priority expansion cannot multiply request work without bound. Generalize `CacheSelectionModel` and `PublicationSelector.current()` to a frozen `MergedSelectionSnapshot` array mapped by the validated identity order per D-01/D-03; do not globally sort across identities. Wire the validated array into `createProductionDependencies`, and include both 17091 and 37091 in the production relay filter. Retain Phase 1 validation, durable admission, and read-only PUT behavior; do not construct signer authentication or public write routes per D-15.</action>
  <verify>
    <automated>deno test --allow-env --allow-read=.,/tmp --allow-write=/tmp tests/integration/operator_config_test.ts tests/integration/publication_selection_test.ts</automated>
  </verify>
  <acceptance_criteria>The focused tests prove a default-plus-named configuration reaches a frozen ordered production snapshot, and malformed, duplicate, or excessive identities cannot reach repository, relay, filesystem, or listener hooks.</acceptance_criteria>
  <done>A production-configured exact identity list produces the first deterministic multi-layer selection end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve independent freshness, expiry, and withdrawal for every layer</name>
  <files>src/nostr/selection.ts, tests/integration/publication_selection_test.ts</files>
  <read_first>src/nostr/selection.ts, src/persistence/state_repository.ts, src/protocol/publication.ts, tests/integration/publication_selection_test.ts</read_first>
  <behavior>
    - "Each identity chooses its own latest NIP-01 winner using created_at then lowest id, without comparing timestamps across identities per D-01/D-03."
    - "Expiry or withdrawal removes only that identity from the exposed snapshot and never exposes its older stored event per D-04."
    - "An update to one identity creates a new frozen array while prior captured arrays and their publication objects remain unchanged."
    - "Restart restoration rebuilds all eligible layers in configured order without weakening per-identity rollback/downgrade watermarks."
  </behavior>
  <action>Refine the custom Applesauce model to group admitted publications by exact raw identity, independently apply the existing NIP-01 tie rule and expiry clock to each group, attach that publisher's BUD-03 projection, and return only current layers in configuration order per D-03/D-04. Schedule the nearest expiry and refresh the model without mutating prior arrays. Keep `StateRepository` authoritative for rollback and signed-to-unsigned policy and preserve corrupt-row isolation. Model withdrawal through the established unavailable/expired selection behavior rather than selecting an older version. Make disposal terminal and idempotent across the source, refresh timer, model subscription, EventStore, and relay wrapper.</action>
  <verify>
    <automated>deno test --allow-read --allow-write tests/integration/publication_selection_test.ts</automated>
  </verify>
  <acceptance_criteria>Tests independently advance, expire, withdraw, restore, and reject rollback for two identities while proving order and prior snapshots remain immutable.</acceptance_criteria>
  <done>Every configured cache layer has independent durable freshness and reactive availability without priority drift.</done>
</task>

</tasks>

## Artifacts this plan produces

- `src/config/config.ts`: bounded canonical ordered default/named identity configuration
- `src/nostr/selection.ts`: immutable ordered multi-identity Applesauce selection model
- `src/runtime/daemon.ts`: production kinds 17091/37091 relay composition
- `tests/integration/operator_config_test.ts`: exact identity and no-side-effect configuration evidence
- `tests/integration/publication_selection_test.ts`: independent freshness/expiry/withdrawal and ordering evidence

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| operator strings -> validated priority list | Invalid, duplicate, or excessive identities could redirect trust or multiply work. |
| relay events -> durable EventStore admission | Hostile data must match an exact configured identity after cryptographic/protocol validation. |
| reactive control plane -> request snapshot | Cross-identity timing and later emissions must not mutate configured priority or captured state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01-01 | Spoofing / Tampering | identity parser and relay admission | high | mitigate | Canonical exact parser, duplicate rejection, author derivation, both-kind relay filter, and exact identity authorization before repository/store admission. |
| T-02-01-02 | Tampering | reactive priority ordering | high | mitigate | Map solely by frozen configuration order; tests vary arrival and cross-identity timestamps. |
| T-02-01-03 | Denial of service | configured identity cardinality | high | mitigate | Compiled maximum checked before side effects and shared downstream request budgets. |
| T-02-01-04 | Tampering | freshness/withdrawal | high | mitigate | Preserve per-identity repository watermarks and remove only the unavailable layer without rollback. |
| T-02-01-SC | Tampering | dependency graph | high | mitigate | No package install or lockfile change; full typecheck uses the existing pinned graph. |
</threat_model>

<verification>
Run `deno task fmt`, `deno task lint`, `deno task check`, both focused integration files, and `deno task test:nix-e2e` to prove the Phase 1 read path remains usable.
</verification>

<success_criteria>
- Ordered canonical default/named identities are the sole priority source.
- Every identity selects and expires independently under existing durable trust policy.
- Production exposes one immutable ordered reactive snapshot and subscribes for both publication kinds.
- Invalid configuration or hostile events cannot influence the merged read surface.
</success_criteria>

## Source Coverage Audit

| Source | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Predictable priority-ordered merged cache | 02-01 | COVERED | Establishes the deterministic ordered snapshot. |
| REQ | PROT-01 | Ordered exact default/named identities | 02-01 | COVERED | Both tasks. |
| RESEARCH | — | Generalize the Applesauce model, preserve durable admission, bound identity count | 02-01 | COVERED | Uses existing stack; no install. |
| CONTEXT | D-01 | Configuration order is sole priority | 02-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-02 | Canonical exact identities and duplicate rejection | 02-01 | COVERED | Task 1. |
| CONTEXT | D-03 | Independent selected roots and immutable ordered snapshot | 02-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-04 | One layer expires/withdraws without rollback | 02-01 | COVERED | Task 2. |
| CONTEXT | D-15 | No signer dependency or public PUT | 02-01 | COVERED | Tracer retains read-only surface. |
</context>

<output>
Create `.planning/phases/02-deterministic-merged-read-cache/02-01-SUMMARY.md` when done.
</output>
