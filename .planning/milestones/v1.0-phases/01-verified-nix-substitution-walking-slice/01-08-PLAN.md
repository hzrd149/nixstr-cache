---
phase: 01-verified-nix-substitution-walking-slice
plan: 08
type: execute
wave: 4
depends_on: [01-01, 01-03]
files_modified:
  - src/network/safe_fetcher.ts
  - src/blossom/blob_fetcher.ts
  - tests/integration/address_pinning_test.ts
  - tests/integration/hostile_blossom_test.ts
autonomous: true
gap_closure: true
requirements: [TREE-03, TREE-05]
must_haves:
  truths:
    - "Publisher-controlled response bodies remain governed through EOF/cancel/error by one total deadline and a resetting idle deadline."
    - "Every resolver answer is parsed canonically and forbidden CIDRs are rejected for compressed, expanded, and IPv4-mapped IPv6 forms on every redirect."
    - "The pinned HTTP/1.1 transport rejects ambiguous/unsupported framing and decodes valid chunked bodies before integrity hashing."
    - "Every exceptional spool exit cancels its response reader and removes the partial owner-only file without quarantining non-hash failures."
  artifacts:
    - path: src/network/safe_fetcher.ts
      provides: "Canonical IP policy and deadline/framing-safe response stream"
      exports: [isForbiddenAddress, PinnedTransport, SafeFetcher]
    - path: src/blossom/blob_fetcher.ts
      provides: "Exceptional response cancellation and deterministic spool cleanup"
  key_links:
    - from: src/network/safe_fetcher.ts
      to: src/blossom/blob_fetcher.ts
      via: "returned body carries total/idle cancellation through the complete spool read"
      pattern: "idleTimeoutMs"
    - from: src/network/safe_fetcher.ts
      to: "Deno.Conn"
      via: "strict decoded HTTP body owns abort listener and socket until terminal state"
      pattern: "Transfer-Encoding"
---

<objective>
Close the hostile-transport gaps without weakening exact-peer pinning, redirect revalidation, or streamed hashing.

Purpose: Ensure a publisher cannot evade SSRF policy, stall a post-header body, smuggle ambiguous HTTP framing, or leave an exceptional spool consuming resources.
Output: Canonical address/CIDR classification, lifecycle deadlines, strict chunk decoding, response cancellation, and adversarial integration tests.

<acceptance_criteria>
- Total timeout applies from request start through response-body termination and idle timeout resets only after actual body progress.
- Expanded IPv6 and IPv4-mapped private/reserved addresses are rejected identically to canonical IPv4/IPv6 forms.
- Transfer-Encoding plus Content-Length, duplicate/conflicting lengths, unsupported codings, malformed chunks/trailers, and premature EOF are rejected; valid split-boundary chunked bodies hash as decoded content.
- Size, abort, disk-write, framing, and timeout failures cancel the body and remove partial spools; only a completed SHA-256 mismatch triggers D-06 quarantine per D-07.
</acceptance_criteria>
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-VERIFICATION.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-01-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry request deadlines and strict HTTP framing through a real spool</name>
  <files>src/network/safe_fetcher.ts, src/blossom/blob_fetcher.ts, tests/integration/hostile_blossom_test.ts</files>
  <read_first>src/network/safe_fetcher.ts, src/blossom/blob_fetcher.ts, tests/integration/address_pinning_test.ts, tests/integration/hostile_blossom_test.ts, src/config/config.ts</read_first>
  <behavior>
    - Test 1: headers followed by a stalled body fail at the total deadline; periodic chunks reset idle time but cannot extend the total deadline.
    - Test 2: a valid chunked response split across arbitrary framing boundaries is decoded to payload bytes before SHA-256 and spool output.
    - Test 3: conflicting Content-Length, Transfer-Encoding plus Content-Length, unsupported coding, invalid chunk size/delimiter/trailer, and truncated final chunk fail closed.
    - Test 4: each exceptional spool read calls reader.cancel with the error and removes the temporary file without quarantine unless a complete payload hash mismatches.
  </behavior>
  <action>Add `idleTimeoutMs` to `SafeFetcherLimits` and carry the combined caller/total signal into the returned response-body stream. Keep the transport abort listener and socket ownership active until close, cancel, or error; wrap body pulls with a resetting idle timer that is rearmed only when bytes arrive and is disposed at terminal state. Parse response framing before constructing the body: accept exactly one valid Content-Length or a final `chunked` transfer coding, reject ambiguous/unsupported combinations, and implement an incremental bounded chunk decoder with strict CRLF, hexadecimal size, extension/trailer ceilings, forbidden trailer fields, zero-chunk termination, and premature-EOF rejection. Never expose framing bytes to BlobFetcher. In BlobFetcher track whether EOF completed successfully; on every exceptional path await reader.cancel(error) before releaseLock, while preserving D-06/D-07 quarantine classification and partial-file cleanup. Avoid whole-body APIs and preserve backpressure one pull at a time.</action>
  <verify>
    <automated>deno test --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/hostile_blossom_test.ts --filter "deadline|chunked|cancel"</automated>
  </verify>
  <done>A real pinned response can be safely decoded and spooled under total/idle deadlines, and every hostile or exceptional terminal path closes network and filesystem resources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonicalize IP literals and enforce forbidden CIDRs</name>
  <files>src/network/safe_fetcher.ts, tests/integration/address_pinning_test.ts</files>
  <read_first>src/network/safe_fetcher.ts, tests/integration/address_pinning_test.ts, NIP.md</read_first>
  <behavior>
    - Test 1: compressed and fully expanded loopback, unspecified, link-local, unique-local, multicast, and documentation IPv6 ranges are rejected.
    - Test 2: dotted and hexadecimal IPv4-mapped IPv6 normalize to IPv4 and reject every existing forbidden IPv4 CIDR.
    - Test 3: malformed, zone-scoped, noncanonical, and mixed DNS answer sets fail closed; representative global addresses remain allowed.
    - Test 4: the exact configured origin retains its explicit local-network exception while publisher sources and redirects do not.
  </behavior>
  <action>Replace prefix/string classification with a project-owned strict parser that converts IPv4 to 32-bit bytes and IPv6 to canonical 128-bit bytes, handles one legal `::` compression, rejects zone identifiers and malformed components, and recognizes both dotted-decimal and hexadecimal IPv4-mapped forms. Normalize mapped addresses to the IPv4 policy, then apply explicit CIDR masks for all forbidden IPv4 and IPv6 ranges required by NIP.md and current tests. Apply classification to every resolver answer on every AddressPolicy.approve call; retain exact-origin configured-local authorization from D-16 and mixed-answer rejection.</action>
  <verify>
    <automated>deno test --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/address_pinning_test.ts</automated>
  </verify>
  <done>Equivalent textual encodings cannot change the address-policy decision, and redirect/DNS revalidation continues to reject any publisher-controlled forbidden answer.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/network/safe_fetcher.ts`: strict canonical IP parser/CIDR matcher, lifecycle-bound deadline stream, incremental HTTP/1.1 chunk decoder
- `src/blossom/blob_fetcher.ts`: exceptional reader cancellation path
- `tests/integration/address_pinning_test.ts`: expanded/mapped IPv6 and CIDR regressions
- `tests/integration/hostile_blossom_test.ts`: total/idle deadline, transfer-framing, chunk-boundary, and cancellation regressions

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| DNS/URL to socket | Publisher-selected targets cross canonical address policy before exact-peer connection |
| socket to decoded body | Untrusted HTTP/1.1 bytes cross strict framing and deadline enforcement |
| decoded body to spool | Payload crosses transfer ceiling and incremental hash before verified storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08-01 | Elevation of privilege | AddressPolicy | high | mitigate | Canonical byte parsing plus CIDR checks for all answers and every redirect; block on expanded/mapped bypass |
| T-01-08-02 | Denial of service | SafeFetcher response body | high | mitigate | Total and resetting idle deadlines remain active through terminal body state |
| T-01-08-03 | Tampering | PinnedTransport framing | high | mitigate | Reject ambiguous framing and strictly decode chunked payload before hash verification |
| T-01-08-04 | Denial of service | BlobFetcher spool | high | mitigate | Cancel exceptional readers and remove partial files; test size/write/abort paths |
| T-01-08-SC | Tampering | dependency graph | high | mitigate | No install task; preserve audited pins and verify the locked graph |
</threat_model>

<verification>
Run `deno task fmt`, `deno task lint`, `deno task check`, `deno task test:address-pinning`, and the complete hostile Blossom integration suite.
</verification>

<success_criteria>
- No publisher URL reaches a forbidden address through textual IPv6 variation, mapping, DNS answers, or redirects.
- No response can outlive its total/idle deadline after headers.
- Only decoded HTTP entity bytes reach integrity hashing and spooling.
- Exceptional spool exits cancel and clean up deterministically.
</success_criteria>

<output>
Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-08-SUMMARY.md` when done.
</output>
