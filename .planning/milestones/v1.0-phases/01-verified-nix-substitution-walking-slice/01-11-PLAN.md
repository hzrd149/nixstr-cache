---
phase: 01-verified-nix-substitution-walking-slice
plan: 11
type: execute
wave: 7
depends_on: [01-07]
files_modified:
  - src/config/config.ts
  - main.ts
  - tests/integration/operator_config_test.ts
autonomous: true
gap_closure: true
requirements: [OPER-01]
must_haves:
  truths:
    - "Signer mode `disabled` with no writable identity is an explicit valid read-only configuration, including when both environment variables are absent."
    - "An enabled supported signer mode is valid only with exactly one syntactically valid writable kind-17091 or kind-37091 raw identity; partial or contradictory write intent fails aggregate validation before repository, relay, filesystem, signer, or listener side effects."
    - "Validated configuration carries a discriminated write-intent value that later Phase 3 composition can consume without interpreting raw strings."
    - "Phase 1 still exposes only the existing GET/HEAD handler; parsing complete write intent does not construct a signer or add/enable PUT endpoints."
  artifacts:
    - path: src/config/config.ts
      provides: "Raw signer fields and validated disabled/enabled write-intent union"
    - path: main.ts
      provides: "NIXSTR_SIGNER_MODE and NIXSTR_WRITABLE_IDENTITY environment mapping"
    - path: tests/integration/operator_config_test.ts
      provides: "read-only defaults, complete intent, partial-config, aggregate diagnostics, and no-side-effect evidence"
  key_links:
    - from: main.ts
      to: src/config/config.ts
      via: "environment fields map unchanged into parseConfig"
      pattern: "NIXSTR_SIGNER_MODE|NIXSTR_WRITABLE_IDENTITY"
    - from: src/config/config.ts
      to: src/app.ts
      via: "createApp parses the full write-intent discriminant before openRepository"
      pattern: "parseConfig\\(raw\\)"
---

<objective>
Close OPER-01 by making operator write intent explicit and safely validated while retaining a read-only Phase 1 runtime.

Purpose: Distinguish intentional read-only operation from malformed partial write configuration before any side effect, and establish the typed configuration boundary Phase 3 will use.
Output: Validated signer-mode/writable-identity fields, environment mapping, and startup-side-effect regression coverage.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-VERIFICATION.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-01-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate explicit read-only and complete write-intent configurations</name>
  <files>src/config/config.ts, tests/integration/operator_config_test.ts</files>
  <read_first>src/config/config.ts, src/app.ts, src/protocol/publication.ts, tests/integration/address_pinning_test.ts, tests/integration/http_cache_test.ts, .planning/REQUIREMENTS.md, NIP.md</read_first>
  <behavior>
    - "Absent signerMode and writableIdentity normalize to `{ mode: 'disabled' }`; explicit `disabled` with absent identity produces the same valid read-only state."
    - "Supported enabled modes `nip46` and `local` require one writable identity; `disabled` plus an identity and an enabled mode without identity each produce diagnostics."
    - "A default identity is exactly `17091:<64-lowercase-hex-pubkey>:` and a named identity is exactly `37091:<64-lowercase-hex-pubkey>:<nonempty-d>`; other kinds, uppercase/short pubkeys, missing/extra separators, or empty named d values fail validation."
    - "Signer/write-intent errors are aggregated with unrelated existing diagnostics and the createApp side-effect tripwires remain untouched."
  </behavior>
  <action>Add `signerMode` and `writableIdentity` raw fields and a readonly discriminated `writeIntent` member on `ValidatedConfig`. Normalize absent mode to `disabled`; accept only `disabled`, `nip46`, or `local`. Parse the writable identity into structured kind/pubkey/identifier data rather than passing a raw string downstream. Require the mode/identity pair to be both disabled-and-absent or enabled-and-present; validate kind 17091 with an empty identifier and kind 37091 with one exact nonempty identifier, using the same raw lowercase pubkey and identity semantics as publication authorization. Aggregate every new diagnostic with the existing validation list and preserve parseConfig purity. This is configuration capability intent only: do not import signers, read key material, contact a bunker, prove ownership, or construct write routes; those side effects and readiness gates belong to Phase 3.</action>
  <verify>
    <automated>deno test --allow-read --allow-write tests/integration/operator_config_test.ts</automated>
  </verify>
  <acceptance_criteria>Read-only defaults and explicit disabled mode validate; each complete supported mode/identity pair produces a typed write intent; every partial, contradictory, or malformed pair fails together with other diagnostics before dependency hooks run.</acceptance_criteria>
  <done>ValidatedConfig unambiguously represents read-only or complete configured write intent without enabling write behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map signer/write identity environment fields without enabling writes</name>
  <files>main.ts, tests/integration/operator_config_test.ts</files>
  <read_first>main.ts, src/app.ts, src/runtime/daemon.ts, src/config/config.ts, src/nix/http_handler.ts, tests/e2e/nix_substitution_test.ts</read_first>
  <behavior>
    - "rawConfigFromEnvironment maps NIXSTR_SIGNER_MODE and NIXSTR_WRITABLE_IDENTITY exactly and preserves absent values."
    - "The shipped launcher starts in read-only mode when both are absent or mode is disabled, retaining the existing GET/HEAD behavior."
    - "A partial environment write configuration exits through validation and does not open repository, relay stream, spool directory, signer connection, or HTTP listener."
    - "Even a complete enabled write intent leaves PUT unavailable in Phase 1 because no signer capability or owned-identity proof is active."
  </behavior>
  <action>Extend `rawConfigFromEnvironment` and the explicit environment allowlist in `main.ts` with `NIXSTR_SIGNER_MODE` and `NIXSTR_WRITABLE_IDENTITY`. Add integration coverage at the exported environment mapper and `createApp`/`launchDaemon` seams proving invalid partial intent returns diagnostics before all injected side-effect hooks, while absent/disabled config launches the unchanged read-only daemon. Assert the production handler still returns its existing method-not-allowed response for PUT even when syntactically complete enabled intent is parsed; do not add a PUT route, signer factory, credential environment variables, or writable readiness state in this phase.</action>
  <verify>
    <automated>deno test --allow-env --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/operator_config_test.ts</automated>
  </verify>
  <acceptance_criteria>The shipped environment boundary exposes both fields, invalid partial intent provably causes no startup side effects, valid read-only operation is explicit, and Phase 1 PUT behavior remains disabled.</acceptance_criteria>
  <done>OPER-01 configuration is complete and safe while signer connection, ownership proof, staging, and PUT remain correctly assigned to Phase 3.</done>
</task>

</tasks>

## Artifacts this plan produces

- `src/config/config.ts`: discriminated read-only/write-intent configuration with strict writable identity parsing
- `main.ts`: `NIXSTR_SIGNER_MODE` and `NIXSTR_WRITABLE_IDENTITY` environment composition
- `tests/integration/operator_config_test.ts`: complete/partial configuration and no-side-effect startup tests

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| environment → validated configuration | Untrusted/mistyped operator strings may accidentally activate or misdirect write intent. |
| validated write intent → Phase 1 runtime | Configuration must not be confused with an active signer, ownership proof, or PUT authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-11-01 | Elevation of privilege | signer mode / writable identity parsing | high | mitigate | Closed mode enum, strict raw identity grammar, discriminated union, and fail-closed pairing tests before side effects. |
| T-01-11-02 | Spoofing | configured owned identity | high | mitigate | Treat parsed identity only as intent; Phase 1 creates no signer or write readiness, and Phase 3 must prove active signer pubkey ownership. |
| T-01-11-03 | Denial of service | startup validation | medium | mitigate | Aggregate diagnostics in pure parseConfig and assert repository/relay/filesystem/listener hooks are untouched on error. |
| T-01-11-04 | Information disclosure | signer configuration | low | accept | These Phase 1 fields contain mode and public cache identity only; no key, bunker secret, or credential field is introduced. |
| T-01-11-SC | Tampering | dependency graph | high | mitigate | No package install or lockfile mutation; existing pinned graph remains unchanged. |
</threat_model>

<verification>
Run `deno task fmt`, `deno task lint`, `deno task check`, the complete operator configuration suite, and `deno task test:nix-e2e` to prove absent signer fields preserve the shipped read path.
</verification>

<success_criteria>
- Absent or explicitly disabled signer configuration is valid and explicitly read-only.
- Supported enabled modes require one strict writable identity; every inconsistent partial state fails before side effects.
- Environment composition exposes both OPER-01 fields as a typed validated intent.
- No Phase 3 signer lifecycle, ownership proof, PUT route, or staging behavior is implemented early.
</success_criteria>

<output>
Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-11-SUMMARY.md` when done.
</output>
