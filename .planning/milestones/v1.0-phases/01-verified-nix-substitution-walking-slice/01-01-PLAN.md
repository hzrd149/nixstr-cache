---
phase: 01-verified-nix-substitution-walking-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deno.json
  - deno.lock
  - src/config/config.ts
  - src/network/safe_fetcher.ts
  - tests/integration/address_pinning_test.ts
autonomous: false
requirements: [TREE-03, OPER-01]
must_haves:
  truths:
    - "Per D-13/D-14/D-15/D-16, invalid configuration reports every diagnostic and causes no listener or outbound connection; required limits have nonzero defaults/hard ceilings; publisher sources reject local/private/reserved answers while the environment origin is explicitly authorized."
    - "Publisher URLs connect only to a DNS-approved public address, while HTTP Host and HTTPS SNI/certificate validation use the original hostname, and every redirect repeats approval."
    - "The address-pinning fixture proves the remote peer address, mixed-answer rejection, redirect-to-private rejection, and DNS-rebinding resistance before SafeFetcher is available to later plans."
  artifacts:
    - path: src/config/config.ts
      provides: "ValidatedConfig, Limits, parseConfig"
    - path: src/network/safe_fetcher.ts
      provides: "AddressPolicy, PinnedTransport, SafeFetcher"
    - path: tests/integration/address_pinning_test.ts
      provides: "blocking address-binding evidence"
  key_links:
    - from: src/network/safe_fetcher.ts
      to: "approved socket peer"
      via: "PinnedTransport connects to the selected IP and retains logical hostname"
    - from: src/config/config.ts
      to: src/network/safe_fetcher.ts
      via: "only the environment-declared preferred origin receives D-16 local-address authorization"
  flagged_assumptions:
    - "OPER-01 unclassified: environment-only configuration is assumed sufficient for the Phase 1 operator workflow."
  prohibitions:
    - statement: "Publisher-controlled URLs must never gain access to loopback, link-local, private, reserved, or mixed-answer destinations through rebinding or redirects."
      status: unverified
      flagged: true
    - statement: "Configuration must never permit a resource limit to be disabled or raised beyond its compiled ceiling."
      status: unverified
      flagged: true
---

## Phase Goal

**As an** operator, **I want to** point a real Nix client at the daemon and substitute an uncached store path from one valid plaintext Nostr cache, **so that** decentralized cache reads preserve trust, integrity, freshness, and bounded resources.

<objective>
Prove the phase's hardest trust boundary and establish the production configuration/transport skeleton.

Purpose: Prevent a validate-then-fetch DNS TOCTOU design from entering the codebase and ensure every later network task consumes a proven safe transport.
Output: Approved exact dependencies, aggregate configuration validation, address-pinned HTTP(S) transport, and hostile-network evidence.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md
@.planning/phases/01-verified-nix-substitution-walking-slice/COVERAGE.md
</context>

## Artifacts this phase produces

- `deno.json`, `deno.lock`
- `src/config/config.ts`: `RawConfig`, `ValidatedConfig`, `Limits`, `ConfigDiagnostic`, `parseConfig`
- `src/network/safe_fetcher.ts`: `SourceTrust`, `ApprovedTarget`, `AddressPolicy`, `PinnedTransport`, `SafeFetcher`, typed network-policy errors
- `tests/integration/address_pinning_test.ts`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve packages flagged by the legitimacy audit</name>
  <read_first>.planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md, deno.json</read_first>
  <action>Pause before any dependency mutation. Review the exact registry entries and upstream ownership/version evidence named below; do not install or resolve a lockfile until the human supplies the resume signal.</action>
  <what-built>No packages have been installed. Research marked applesauce-core@6.2.0, applesauce-relay@6.2.1, applesauce-loaders@6.2.0, @noble/hashes@2.3.0, and @scure/base@2.3.0 as SUS rather than SLOP.</what-built>
  <how-to-verify>Open each exact npm package page, confirm package ownership/source correspondence with the official Applesauce or Paul Miller repositories, confirm the named version and absence of unexpected install scripts, then approve those exact pins. Also inspect the official JSR page for @db/sqlite@0.13.0 and acknowledge its FFI/native-library permission boundary.</how-to-verify>
  <verify><automated>rg -n '^\| `(applesauce-core|applesauce-relay|applesauce-loaders|@noble/hashes|@scure/base)`.*\| SUS' .planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md</automated><human-check>Record approval of every SUS package and the SQLite FFI boundary.</human-check></verify>
  <acceptance_criteria>Human explicitly approves or replaces every SUS dependency before `deno add` or lockfile resolution occurs.</acceptance_criteria>
  <done>The exact allowed package/version set and SQLite FFI acknowledgement are recorded for Task 2.</done>
  <resume-signal>Type "approved" with the accepted exact versions, or name rejected packages and approved replacements.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Prove one address-pinned outbound request end to end</name>
  <precondition>The package-legitimacy checkpoint is approved.</precondition>
  <reversibility rating="costly">Later Blossom reads depend on the PinnedTransport contract; changing it would touch the network, fixture, and resolver seams.</reversibility>
  <read_first>AGENTS.md, NIP.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md, deno.json, main.ts</read_first>
  <files>deno.json, deno.lock, src/config/config.ts, src/network/safe_fetcher.ts, tests/integration/address_pinning_test.ts</files>
  <behavior>
    - "A controlled HTTP request connects to the selected approved peer IP and sends the original hostname in Host."
    - "A controlled HTTPS request connects to the selected approved peer IP while validating SNI/certificate against the original hostname."
    - "Publisher sources reject any forbidden or mixed DNS answer; the D-16 configured origin alone may use its resolved local address."
    - "Redirect targets are parsed, resolved, classified, and pinned again; rebinding between approval and connection cannot change the peer."
  </behavior>
  <action>First add a failing integration suite whose server records the accepted peer and Host/SNI identity. Pin only checkpoint-approved versions in `deno.json`/`deno.lock`. Implement `parseConfig` as a side-effect-free aggregate validator with the research defaults/ceilings and D-13/D-14/D-16 rules. Implement `PinnedTransport` using a Deno API or a narrow HTTP/TLS transport that binds the connection to an approved address; ordinary global fetch after DNS preflight is not an acceptable implementation. Build `SafeFetcher` around manual redirects, per-hop policy, explicit deadlines, response-body cancellation, and an injected resolver/transport for deterministic tests. If Deno cannot satisfy address binding plus hostname validation, stop this plan as blocked and do not expose a weaker SafeFetcher.</action>
  <verify><automated>deno test --allow-net=127.0.0.1 --allow-read --allow-write tests/integration/address_pinning_test.ts</automated></verify>
  <acceptance_criteria>The test observes the exact approved peer address; Host/SNI/certificate identity remains the URL hostname; redirect and rebinding fixtures cannot reach forbidden peers; invalid config aggregates errors and performs no I/O.</acceptance_criteria>
  <done>A production-quality address-pinned request path exists and later Blossom code can depend on it without a DNS TOCTOU gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| environment → daemon | Operator configuration controls bind address, trusted relays/source, and hard resource bounds. |
| publisher URL/DNS → outbound socket | Hostile names, DNS answers, redirects, and responses attempt to reach privileged networks or exhaust resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation / Information disclosure | `SafeFetcher` | high | mitigate | Address classification plus socket address pinning and per-hop redirect revalidation; the automated fixture is a blocking proof. |
| T-01-02 | Denial of service | config and fetch deadlines | high | mitigate | Nonzero defaults, compiled ceilings, connection/idle/total deadlines, cancellation, redirect and concurrency limits. |
| T-01-03 | Tampering | dependency graph | high | mitigate | Exact pins/lockfile and blocking human approval for every SUS package before install. |
| T-01-SC | Tampering | npm installs | high | mitigate | Package legitimacy checkpoint and exact registry/version verification. |
</threat_model>

<verification>
`deno fmt --check && deno lint && deno check src/config/config.ts src/network/safe_fetcher.ts` and the address-pinning integration test pass. Any failure to prove actual-peer pinning blocks all dependent plans.
</verification>

<success_criteria>
The operator-facing configuration boundary is side-effect-free until valid, and the only publisher-controlled transport available to subsequent plans is demonstrably address-pinned.
</success_criteria>

<output>Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-01-SUMMARY.md` when done.</output>
