---
phase: 01-verified-nix-substitution-walking-slice
plan: 10
type: execute
wave: 7
depends_on: [01-03, 01-07]
files_modified:
  - src/nostr/selection.ts
  - src/nostr/blossom_servers.ts
  - src/runtime/daemon.ts
  - tests/integration/publication_selection_test.ts
  - tests/integration/blossom_discovery_test.ts
autonomous: true
gap_closure: true
requirements: [PROT-03, TREE-01]
must_haves:
  truths:
    - "Only cryptographically and protocol-valid, allow-listed cache publications become EventStore events, and durable repository acceptance completes before the custom Applesauce model can emit them."
    - "The project custom Applesauce model is the authoritative reactive selection surface; expiry, restart restoration, rollback/downgrade rejection, repository failure, and disposal retain their existing behavior without a BehaviorSubject selection authority (D-01/D-02)."
    - "A valid latest kind 10063 event from the selected allow-listed publisher reactively contributes its authenticated `server` URLs after configured and publication-tag sources, in published order with deduplication (D-05)."
    - "Malformed, invalidly signed, foreign-publisher, stale, or unsafe BUD-03 data cannot influence production source resolution, and discovered URLs retain publisher trust so D-15 network checks apply."
  artifacts:
    - path: src/nostr/selection.ts
      provides: "EventStore-backed durable publication admission and custom CacheSelectionModel"
      exports: [startPublicationSelection, CacheSelectionModel]
    - path: src/nostr/blossom_servers.ts
      provides: "Strict authenticated kind-10063 admission and ordered server-tag projection"
    - path: src/runtime/daemon.ts
      provides: "One Applesauce relay/store control plane feeding production selection and source resolution"
    - path: tests/integration/blossom_discovery_test.ts
      provides: "Production relay-to-source-plan coverage and malformed-list regressions"
  key_links:
    - from: src/nostr/selection.ts
      to: src/persistence/state_repository.ts
      via: "StateRepository.accept succeeds before EventStore.add for a publication"
      pattern: "repository\\.accept[\\s\\S]*store\\.add"
    - from: src/nostr/selection.ts
      to: applesauce-core
      via: "EventStore.model(CacheSelectionModel, ...) supplies selected$ and current snapshot state"
      pattern: "\\.model\\(CacheSelectionModel"
    - from: src/runtime/daemon.ts
      to: src/blossom/source_plan.ts
      via: "the snapshot's reactive BUD-03 URLs are passed as buildSourcePlan.bud03"
      pattern: "bud03:.*bud03"
---

<objective>
Close PROT-03 and TREE-01 by making validated Nostr state genuinely Applesauce-reactive and carrying authenticated BUD-03 discovery into each immutable read snapshot.

Purpose: Remove the standalone selection subject and the production-orphaned BUD-03 source tier while preserving every already-proven trust, durability, freshness, and shutdown invariant.
Output: EventStore-backed custom selection model, strict kind-10063 projection, production source-plan wiring, and focused integration evidence.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-VERIFICATION.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-02-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-03-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive one durably accepted publication through EventStore and CacheSelectionModel</name>
  <files>src/nostr/selection.ts, tests/integration/publication_selection_test.ts</files>
  <read_first>src/nostr/selection.ts, src/protocol/publication.ts, src/persistence/state_repository.ts, tests/integration/publication_selection_test.ts, /home/user/.cache/deno/npm/registry.npmjs.org/applesauce-core/6.2.0/dist/event-store/event-store.d.ts, /home/user/.cache/deno/npm/registry.npmjs.org/applesauce-core/6.2.0/dist/event-store/event-models.d.ts, /home/user/.cache/deno/npm/registry.npmjs.org/applesauce-core/6.2.0/dist/event-store/interface.d.ts</read_first>
  <behavior>
    - "A valid allow-listed event is absent from EventStore/model emissions until StateRepository.accept has committed, then becomes the immutable current selection."
    - "Invalid, unauthorized, stale, tie-losing, downgrade-blocked, and transaction-failed events never enter EventStore."
    - "Restored valid selections are revalidated and allow-listed before EventStore admission; expiry removes serving availability without admitting an older event per D-01/D-02."
    - "Disposal unsubscribes relay admission, completes the selected model view, cancels expiry work, and calls EventStore.dispose exactly once."
  </behavior>
  <action>Replace the BehaviorSubject authority with a daemon-owned `EventStore` from the installed `applesauce-core@6.2.0`. Export a project custom `CacheSelectionModel` using the installed `Model`/`ModelConstructor` contract and obtain its observable via `EventStore.model`; the model must derive only from events admitted to that store and expose immutable `SelectedPublication` snapshots. Keep `validatePublication`, publisher/identity allow-lists, and `StateRepository.accept` ahead of `EventStore.add`; a failed or rejected repository result must leave the store unchanged. Revalidate restored rows through the same authorization boundary before store admission. Preserve NIP-01 lowest-id ties even if Applesauce's generic replaceable ordering differs, preserve explicit D-01 expiry clearing without rollback, and retain synchronous `current()` by subscribing to the replaying custom model rather than maintaining a parallel authoritative subject. Dispose the source subscription, expiry schedule, model subscription, and EventStore terminally and idempotently. Do not make EventStore persistence responsible for anti-rollback or downgrade policy; the domain repository remains authoritative for those security properties.</action>
  <verify>
    <automated>deno test --allow-read --allow-write tests/integration/publication_selection_test.ts</automated>
  </verify>
  <acceptance_criteria>All existing publication-selection cases pass, new assertions observe `EventStore.add` only after repository commit and observe the custom model as the selected$ source, and `rg -n 'BehaviorSubject' src/nostr/selection.ts` returns no matches.</acceptance_criteria>
  <done>PROT-03 selection is exposed through a project custom Applesauce model without weakening durable admission, authorization, freshness, expiration, downgrade, or shutdown behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reactively admit and project authenticated BUD-03 server lists</name>
  <files>src/nostr/blossom_servers.ts, src/nostr/selection.ts, tests/integration/blossom_discovery_test.ts</files>
  <read_first>NIP.md, src/nostr/selection.ts, src/protocol/publication.ts, src/blossom/source_plan.ts, tests/integration/publication_selection_test.ts, /home/user/.cache/deno/npm/registry.npmjs.org/applesauce-core/6.2.0/dist/event-store/event-store.d.ts, /home/user/.cache/deno/npm/registry.npmjs.org/applesauce-core/6.2.0/dist/event-store/event-models.d.ts</read_first>
  <behavior>
    - "A cryptographically valid kind-10063 event by an allow-listed selected publisher contributes exact ordered `server` tag URLs to the reactive snapshot."
    - "The latest valid replacement wins; an older or equal-time losing list cannot replace it, and a new accepted list updates the snapshot without changing its selected publication."
    - "Wrong kind, invalid signature/id, non-whitelisted publisher, malformed tag shape, credentials, non-HTTP(S), query, or fragment entries do not enter the usable BUD-03 projection."
    - "Invalid individual server entries are ignored without reordering remaining authenticated entries; an event with no usable entries contributes an empty list."
  </behavior>
  <action>Create a narrow project-owned kind-10063 validator/projector using the locked `nostr-tools` verifier and exact BUD-03 `server` tags. Validate event authenticity, exact kind, publisher allow-list, replaceable ordering, and URL shape before store use; keep valid ordered URL strings as data on the custom selection snapshot. Extend `CacheSelectionModel` to combine the selected cache publication with the same publisher's latest EventStore-backed kind-10063 replacement. Preserve immutable snapshots so an in-flight Nix request retains its captured BUD-03 list per D-11. Do not add `applesauce-common` or another dependency: the installed EventStore APIs plus the small strict BUD-03 projection are sufficient and avoid an unaudited package change.</action>
  <verify>
    <automated>deno test --allow-read --allow-write tests/integration/blossom_discovery_test.ts --filter "BUD-03|server list|malformed"</automated>
  </verify>
  <acceptance_criteria>Authenticated ordered BUD-03 lists update the custom selection snapshot, while malformed, untrusted, and stale list data never appears in its usable server list.</acceptance_criteria>
  <done>The EventStore-backed selected snapshot reacts to trusted publisher BUD-03 replacements and carries only strict ordered source URLs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire kind-10063 relay events into production source resolution</name>
  <files>src/runtime/daemon.ts, tests/integration/blossom_discovery_test.ts</files>
  <read_first>src/runtime/daemon.ts, src/nostr/selection.ts, src/nostr/blossom_servers.ts, src/blossom/source_plan.ts, src/network/safe_fetcher.ts, tests/e2e/nix_substitution_test.ts</read_first>
  <behavior>
    - "Production subscribes for kinds 17091 and 10063 from configured publisher pubkeys through one RelayPool lifecycle."
    - "A production-composed request uses configured URL, selected event blossom tags, then current authenticated BUD-03 URLs, deduplicated in that order per D-05."
    - "A reactive kind-10063 replacement changes subsequent request source plans while an already captured request snapshot remains unchanged per D-11."
    - "BUD-03 URLs remain publisher-controlled sources and are rejected by existing DNS/redirect policy for local, private, reserved, or mixed answers per D-15."
  </behavior>
  <action>Extend `createPublicationEventStream` to request both required kinds for configured authors and pass the raw stream into the unified selection/control-plane admission. In `createProductionDependencies`, keep one EventStore owned by the selection lifecycle, expose the custom model snapshot through `current()`, and pass `snapshot.bud03Servers` to `buildSourcePlan({ configured, event, bud03, isQuarantined })`. Ensure shutdown closes selector/model/store before the relay pool and remains idempotent. Add a production-wiring integration fixture using the real `createProductionDependencies` seam (not a direct helper-only call) that emits a cache publication plus kind-10063 replacements and records attempted origins/order. Preserve the existing configured-origin exception only for `configured`; event-tag and BUD-03 candidates retain `publisher` trust and receive SafeFetcher DNS/redirect revalidation.</action>
  <verify>
    <automated>deno test --allow-env --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/blossom_discovery_test.ts</automated>
  </verify>
  <acceptance_criteria>A production-composed handler receives live authenticated kind-10063 state and attempts configured, event-tag, then BUD-03 sources; reactive replacement, snapshot immutability, malformed-list handling, and publisher network policy are automated.</acceptance_criteria>
  <done>TREE-01 is no longer helper-only: valid publisher BUD-03 discovery reaches buildSourcePlan in production without crossing whitelist or SSRF trust boundaries.</done>
</task>

</tasks>

## Artifacts this plan produces

- `src/nostr/selection.ts`: EventStore-owned durable admission and `CacheSelectionModel`
- `src/nostr/blossom_servers.ts`: authenticated kind-10063 validation and ordered projection
- `src/runtime/daemon.ts`: kinds 17091/10063 subscription and BUD-03 source-plan wiring
- `tests/integration/publication_selection_test.ts`: repository-before-store/model and lifecycle regressions
- `tests/integration/blossom_discovery_test.ts`: production BUD-03 discovery, ordering, update, and malformed-input evidence

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| relay events → trusted EventStore | Hostile relay data must pass cryptographic, protocol, publisher, identity, and durable policy gates before admission. |
| publisher kind-10063 tags → outbound source plan | Authenticated publisher URLs remain untrusted network destinations and cannot inherit configured-origin authority. |
| reactive store → request snapshot | Concurrent relay updates must not mutate the publication/source set already captured by a request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10-01 | Spoofing / Tampering | publication and kind-10063 admission | high | mitigate | Verify signature/id, kind, allow-listed pubkey/identity, domain policy, and repository commit before EventStore admission; assert rejected data is absent from store/model. |
| T-01-10-02 | Tampering | reactive selection ordering | high | mitigate | Custom model preserves NIP-01 lowest-id tie policy, D-01 expiry, durable rollback/downgrade state, and immutable snapshots under tests. |
| T-01-10-03 | Information disclosure / Elevation | BUD-03 source URLs | high | mitigate | Strict URL projection plus publisher trust classification routes every discovered destination through existing D-15 address and redirect checks. |
| T-01-10-04 | Denial of service | server-list churn/cardinality | medium | mitigate | EventStore replacement semantics retain only the latest list, malformed entries are dropped, and existing source-attempt ceilings bound fetch work. |
| T-01-10-SC | Tampering | dependency graph | high | mitigate | No package install; use exact locked applesauce-core 6.2.0 and existing lockfile, with `deno task check` proving resolved APIs. |
</threat_model>

<verification>
Run `deno task fmt`, `deno task lint`, `deno task check`, the complete publication-selection suite, the complete BUD-03 production-wiring suite, and `deno task test:nix-e2e`.
</verification>

<success_criteria>
- Validated durable publications enter EventStore and selection is emitted by `CacheSelectionModel`, not a BehaviorSubject.
- All existing freshness, rollback, downgrade, expiry, restore, allow-list, atomicity, and shutdown guarantees still pass.
- Valid latest publisher kind-10063 lists update production source plans in D-05 order.
- Malformed/untrusted BUD-03 data and unsafe discovered destinations cannot influence successful resolution.
</success_criteria>

## Source Coverage Audit

| Source | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe reactive plaintext Nostr/Blossom substitution | 01-10 | COVERED | Replaces the two remaining disconnected control-plane links without changing the proven read path. |
| REQ | PROT-03 | EventStore/custom reactive model with atomic durable selection | 01-10 | COVERED | Tasks 1-2. |
| REQ | TREE-01 | Event tags plus publisher BUD-03 and configured sources | 01-10 | COVERED | Tasks 2-3, per D-05/D-15. |
| REQ | OPER-01 | Signer mode and writable identity validation | 01-11 | COVERED | Separate same-wave configuration plan. |
| RESEARCH | — | Applesauce EventStore/model and kind-10063 control-plane composition | 01-10 | COVERED | Uses already locked 6.2.0 APIs and no install. |
| CONTEXT | D-01/D-02 | Expiry and restart anti-rollback behavior | 01-10 | COVERED | Task 1 preserves existing verified behavior. |
| CONTEXT | D-05/D-15 | Source order and publisher URL network restrictions | 01-10 | COVERED | Tasks 2-3. |
| CONTEXT | D-11 | Immutable per-request selection snapshot | 01-10 | COVERED | Tasks 2-3. |
| CONTEXT | deferred | Local verified Blossom write-through | — | EXCLUDED | Explicitly Phase 2. |
</context>

<output>
Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-10-SUMMARY.md` when done.
</output>
