---
phase: 01-verified-nix-substitution-walking-slice
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/protocol/nhash.ts
  - src/protocol/publication.ts
  - src/persistence/state_repository.ts
  - src/nostr/selection.ts
  - tests/protocol/publication_test.ts
  - tests/integration/publication_selection_test.ts
autonomous: true
requirements: [PROT-02, PROT-03, PROT-04, PROT-05, PROT-06]
must_haves:
  truths:
    - "Only fully verified, unexpired NIP.md publications with a canonical plaintext `nhash` can enter Applesauce state or selection."
    - "Per D-01, expiry clears serving; per D-02, restart retains a newer persisted root; per D-03, missing blobs affect only that path; per D-04, a newer valid publication is not rolled back after a later corrupt or unavailable path."
    - "Selection emits only after one SQLite transaction commits event, `(created_at,id)` watermark/tie state, signed history, and consent transition."
    - "A restart restores the greatest accepted selection and rejects older/tie-losing relay candidates; signed-to-unsigned transition requires durable explicit consent."
  artifacts:
    - path: src/protocol/publication.ts
      provides: "ValidatedPublication boundary and rejection taxonomy"
    - path: src/persistence/state_repository.ts
      provides: "transactional publication/policy repository"
    - path: src/nostr/selection.ts
      provides: "reactive immutable SelectedPublication snapshots"
  key_links:
    - from: src/protocol/publication.ts
      to: src/nostr/selection.ts
      via: "raw relay events are validated before EventStore admission"
    - from: src/nostr/selection.ts
      to: src/persistence/state_repository.ts
      via: "commit precedes reactive snapshot emission"
  flagged_assumptions:
    - "PROT-02 adjacency: events exactly 15 minutes in the future and exactly at expiration require boundary fixtures."
    - "PROT-02 empty: empty content is valid, but empty required tag values are rejected according to NIP.md."
    - "PROT-02 ordering: validation always precedes store admission and candidate comparison."
    - "PROT-03 unclassified: Applesauce custom casts are assumed to expose the required atomic immutable snapshot cleanly."
    - "PROT-04 unclassified: SQLite `(created_at,id)` persistence is assumed to reproduce adopted NIP-01 tie ordering across restart."
    - "PROT-05 unclassified: explicit downgrade consent is represented as durable per-raw-identity policy state."
    - "PROT-06 unclassified: any type-5 key is rejected as unsupported rather than treated as malformed plaintext."
  prohibitions:
    - statement: "A syntactically valid but cryptographically unverified event must never become observable through the reactive store."
      status: unverified
      flagged: true
    - statement: "Expiry, restart, relay withholding, or a corrupt path must never silently select an older publication."
      status: unverified
      flagged: true
    - statement: "An identity that has declared signing keys must never become unsigned without recorded operator consent."
      status: unverified
      flagged: true
---

## Phase Goal

**As an** operator, **I want to** point a real Nix client at the daemon and substitute an uncached store path from one valid plaintext Nostr cache, **so that** decentralized cache reads preserve trust, integrity, freshness, and bounded resources.

<objective>
Deliver the verified, durable, reactive control-plane slice for one raw cache identity.

Purpose: Make the selected root a trustworthy immutable input to every HTTP request, including after restart and hostile relay behavior.
Output: Strict publication/nhash codecs, SQLite policy state, Applesauce/RxJS selection, and restart/expiry/downgrade tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@NIP.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `src/protocol/nhash.ts`: `PlaintextRoot`, `decodePlaintextNhash`
- `src/protocol/publication.ts`: `RawPublication`, `ValidatedPublication`, `PublicationRejection`, `validatePublication`, `cacheIdentity`
- `src/persistence/state_repository.ts`: `StateRepository`, `StoredSelection`, `IdentityPolicy`, SQLite migrations/repository methods
- `src/nostr/selection.ts`: `SelectedPublication`, `PublicationSelector`, `startPublicationSelection`
- `tests/protocol/publication_test.ts`, `tests/integration/publication_selection_test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate one plaintext publication before admission</name>
  <read_first>NIP.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md, deno.json</read_first>
  <files>src/protocol/nhash.ts, src/protocol/publication.ts, tests/protocol/publication_test.ts</files>
  <behavior>
    - "Every NIP.md rejection condition, NIP-40 expiration, future-time boundary, duplicate tag, raw identity, and canonical base64/Bech32/TLV case has a fixture."
    - "Invalid blossom tags are ignored individually; valid tags retain order."
    - "Exactly one 32-byte type-0 TLV succeeds; type 5 is a typed unsupported-BUD-15 rejection; legacy/unknown/duplicate records fail."
  </behavior>
  <action>Write fixed and property tests first. Implement strict `decodePlaintextNhash` with canonical re-encoding and exact TLV consumption. Implement `validatePublication` so NIP-01 id/signature verification and all NIP.md validation complete before a branded immutable value can be created. Preserve raw `d` identity bytes, validate `nixSigKey` names/canonical key bytes without treating names as identity, honor expiration, and return structured rejection reasons suitable for operator reporting.</action>
  <verify><automated>deno test tests/protocol/publication_test.ts</automated></verify>
  <acceptance_criteria>All normative invalid cases remain unconstructable as `ValidatedPublication`; valid plaintext default/named fixtures preserve identity, keys, and ordered source tags.</acceptance_criteria>
  <done>Untrusted relay events cross a single exhaustive validation boundary into a typed plaintext publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit selection policy before reactive exposure</name>
  <reversibility rating="costly">The durable identity/watermark schema is consumed by every later phase, though versioned migrations keep it changeable.</reversibility>
  <read_first>NIP.md, src/protocol/publication.ts, .planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md</read_first>
  <files>src/persistence/state_repository.ts, src/nostr/selection.ts, tests/integration/publication_selection_test.ts</files>
  <behavior>
    - "Latest candidate uses `(created_at,event.id)` ordering; older/tie-losing events never replace durable selection."
    - "Selection emission occurs after transaction commit and restart restores the committed root."
    - "Expiration clears current availability without choosing older state; later path failures do not mutate selection."
    - "Signed history blocks unsigned candidates until explicit per-identity consent is stored."
  </behavior>
  <action>Create a narrow SQLite repository whose migration and transactions persist the complete selected event, watermark/tie id, signed-history bit, consent, and later quarantine table. Feed verified relay Observables through Applesauce `EventStore` and a custom reactive selector; never add rejected events to the store. Compare by the adopted NIP-01 tuple, commit policy/selection atomically, and only then emit a frozen snapshot. Restore persisted selection on startup per D-02, schedule expiration eligibility per D-01, and expose teardown that disposes subscriptions/store.</action>
  <verify><automated>deno test --allow-read --allow-write tests/integration/publication_selection_test.ts</automated></verify>
  <acceptance_criteria>Crash/restart, stale relay, equal-timestamp tie, expiry, downgrade-without-consent, downgrade-with-consent, and transaction-failure tests prove D-01–D-04 and PROT-03–PROT-05.</acceptance_criteria>
  <done>One eligible plaintext root is durably and reactively selectable without rollback or silent downgrade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| relay event → validated publication | Hostile signed/unsigned/malformed data attempts to enter trusted state. |
| reactive memory → SQLite | Crash and ordering races can expose state that is not durably committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Spoofing / Tampering | publication validator | high | mitigate | Verify NIP-01 id/signature and strict tags/nhash before EventStore admission. |
| T-01-05 | Tampering | selection/watermark | high | mitigate | Transactional `(created_at,id)` watermark and commit-before-emit ordering with restart tests. |
| T-01-06 | Elevation | signed downgrade | high | mitigate | Durable signed-history bit and explicit per-identity consent gate. |
| T-01-07 | Denial of service | event/tag parsing | medium | mitigate | Length/multiplicity limits and strict bounded decoders on the primary relay boundary. |
| T-01-SC | Tampering | npm installs | high | mitigate | Uses only exact packages approved in 01-01 and the committed lockfile. |
</threat_model>

<verification>
`deno fmt --check && deno lint && deno check src/protocol/nhash.ts src/protocol/publication.ts src/persistence/state_repository.ts src/nostr/selection.ts && deno test tests/protocol/publication_test.ts && deno test --allow-read --allow-write tests/integration/publication_selection_test.ts`
</verification>

<success_criteria>
The daemon has one validated, durable, immutable reactive publication snapshot and rejects every stale, expired, downgrade, encrypted, or malformed candidate without exposing it.
</success_criteria>

<output>Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-02-SUMMARY.md` when done.</output>
