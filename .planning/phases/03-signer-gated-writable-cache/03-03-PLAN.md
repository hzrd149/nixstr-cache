---
phase: 03-signer-gated-writable-cache
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/persistence/write_repository.ts
  - src/write/batch_scheduler.ts
  - src/hashtree/writer.ts
  - src/protocol/hashtree.ts
  - src/runtime/daemon.ts
  - tests/protocol/hashtree_writer_test.ts
  - tests/integration/publication_batch_test.ts
autonomous: true
requirements: [PUBL-01, PUBL-02]
must_haves:
  truths:
    - "PUBL-01: the first eligible dirty event opens one batch window; five quiet seconds or sixty sustained seconds claims it exactly once, and only one freeze/build worker runs at a time."
    - "PUBL-02: each claimed batch freezes a dependency-closed overlay generation and base committed root, then deterministically builds a plaintext canonical BUD-16/17/18 copy-on-write tree."
    - "The root and complete reachable inventory survive restart only as an unpublished pending candidate; committed signer reads do not switch roots and Phase 3 performs no replication, signing, or relay publication."
  artifacts:
    - path: "src/write/batch_scheduler.ts"
      provides: "Injectable-clock 5s quiet/60s maximum serialized generation scheduler"
      exports: ["PublicationBatchScheduler"]
    - path: "src/hashtree/writer.ts"
      provides: "Streaming canonical plaintext persistent copy-on-write Hashtree builder"
      exports: ["HashtreeWriter"]
    - path: "src/persistence/write_repository.ts"
      provides: "Durable frozen batches, candidate blobs, and unpublished pending-candidate contract"
    - path: "tests/protocol/hashtree_writer_test.ts"
      provides: "Pinned vectors, boundary, determinism, reuse, and streaming evidence"
  key_links:
    - from: "src/write/batch_scheduler.ts"
      to: "src/persistence/write_repository.ts"
      via: "generation-token compare-and-claim freezes closure/base root once"
      pattern: "claim|freeze"
    - from: "src/hashtree/writer.ts"
      to: "src/protocol/hashtree.ts"
      via: "canonical encoder emits bytes accepted byte-for-byte by strict decoder and pinned vectors"
      pattern: "encode.*Manifest"
    - from: "src/hashtree/writer.ts"
      to: "src/persistence/write_repository.ts"
      via: "atomic pending candidate records root plus complete reachable blob inventory without updating committed overlay"
      pattern: "pending"
---

<objective>
Freeze eligible writes on deterministic quiet/max-delay boundaries and build a durable unpublished plaintext Hashtree candidate per D-01, D-05, and D-06, implementing PUBL-01 and PUBL-02.

Purpose: Complete Phase 3's third durable boundary while leaving every availability, signing, and publication side effect for Phase 4.
Output: Serialized scheduler, canonical streaming copy-on-write writer, durable pending-candidate inventory, restart recovery, and protocol/integration proofs.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-signer-gated-writable-cache/03-CONTEXT.md
@.planning/phases/03-signer-gated-writable-cache/03-RESEARCH.md
@.planning/phases/03-signer-gated-writable-cache/03-02-SUMMARY.md
@src/protocol/hashtree.ts
@src/protocol/nhash.ts
@src/hashtree/reader.ts

<interfaces>
Consume Plan 03-02's durable immutable overlay generations. The strict reader accepts canonical BUD manifest types 1 (file), 2 (directory), and 3 (fanout), at configured link/metadata bounds. Preserve separate APIs for committed overlay state and `pending_candidate`; Phase 4 alone may claim and publish pending state.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one quiet eligible generation into one durable unpublished root</name>
  <files>src/persistence/write_repository.ts, src/write/batch_scheduler.ts, src/hashtree/writer.ts, src/protocol/hashtree.ts, tests/protocol/hashtree_writer_test.ts, tests/integration/publication_batch_test.ts</files>
  <behavior>
    - One eligible generation opens a window and fake-clock advancement to five quiet seconds durably claims exactly that generation once.
    - The writer builds canonical plaintext manifests bottom-up, persists each immutable blob and reachable inventory, and records one pending nhash/root transactionally.
    - The committed signer overlay remains the same generation/root before and after pending candidate creation, and no network/signing collaborator is invoked.
  </behavior>
  <reversibility rating="costly">The canonical on-disk BUD bytes and pending-candidate handoff become Phase 4 inputs; changing either requires coordinated compatibility and persistence work.</reversibility>
  <action>Add a project-owned canonical manifest encoder paired with the existing strict decoder and pinned to the adopted BUD-16/17/18 revisions. Implement `HashtreeWriter` over a frozen sorted logical path map: stream files in exact 2,097,152-byte chunks, incrementally hash and persist immutable chunks, cap links at 174, sort directory names by ascending UTF-8 bytes, emit type-3 fanout nodes at boundaries, encode child nodes before parents with the prescribed `l,t` and `h,k,m,n,s,t` field ordering/shortest integers/binary hashes, and reuse base links whenever the logical subtree hash is unchanged. Never materialize an unbounded file or full dataset; use bounded per-node arrays plus durable iteration. Implement a scheduler with injected clock/timer seams and a persisted monotonic window token: clean→dirty records openedAt, quiet resets to last eligible+5s, maximum remains openedAt+60s, firing cancels both and atomically claims the generation/base committed root. Serialize builds through one promise chain. Persist batch, candidate blob inventory, root hash, canonical plaintext nhash, counts, and bytes as pending only after the entire reachable set is durable. Do not mutate the committed overlay/read root.</action>
  <verify>
    <automated>deno test --allow-read=.,/tmp --allow-write=/tmp tests/protocol/hashtree_writer_test.ts tests/integration/publication_batch_test.ts --filter "quiet window builds one unpublished pending candidate"</automated>
  </verify>
  <done>One five-second quiet window creates exactly one deterministic, reader-compatible, durable pending candidate and leaves the committed read view unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close sustained-write, serialization, copy-on-write, restart, and phase-boundary cases</name>
  <files>src/persistence/write_repository.ts, src/write/batch_scheduler.ts, src/hashtree/writer.ts, src/runtime/daemon.ts, tests/protocol/hashtree_writer_test.ts, tests/integration/publication_batch_test.ts</files>
  <behavior>
    - Continuous eligible arrivals fire once at 60 seconds; writes after durable freeze enter the next window, and quiet/max races cannot duplicate a batch.
    - Two builds of identical frozen input produce identical root/inventory bytes; a one-path update reuses unchanged base subtrees and only adds newly reachable blobs.
    - Restart recovers pending state and resumes/marks an interrupted building batch deterministically without exposing it; source inspection proves no Blossom upload, signEvent, or relay publish occurs.
  </behavior>
  <action>Wire eligible-generation events from the overlay into the serialized scheduler and drain/cancel timers and the active worker in daemon shutdown order. Use generation-token compare-and-claim transactions so stale timers and simultaneous quiet/max callbacks cannot claim twice; after a worker completes, open the next window if post-freeze writes are dirty. Define deterministic restart semantics for `building`, `failed`, and `pending` rows: rebuild safely from the immutable frozen generation or retain the previous pending candidate, never synthesize committed state. Add golden canonical vectors, exact chunk/link threshold cases, bytewise Unicode ordering, build-twice equality, base reuse, sustained 60-second timing, post-freeze next-batch, race, worker serialization, and restart tests. Assert the pending inventory is complete and dependency-closed. Add production negative gates scoped to signer/writer/scheduler/daemon call regions proving Phase 3 does not invoke signing, Blossom upload, relay publication, or update the committed read root from pending state.</action>
  <verify>
    <automated>deno test --allow-read=.,/tmp --allow-write=/tmp --allow-net=127.0.0.1 tests/protocol/hashtree_writer_test.ts tests/integration/publication_batch_test.ts</automated>
  </verify>
  <done>PUBL-01/PUBL-02 hold under quiet, sustained, racing, concurrent, restart, and COW-reuse cases; the pending candidate is complete and deterministic yet unpublished and invisible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reactive dirty events→durable batch claim | Concurrent arrivals and timers choose immutable batch membership. |
| frozen overlay/files→canonical Hashtree blobs | Potentially large local staged data becomes content-addressed protocol output. |
| pending candidate→future Phase 4 | Unavailable unpublished state must not become authoritative read/publication state. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering | `src/write/batch_scheduler.ts` | high | mitigate | Persisted monotonic token and atomic compare-and-claim prevent stale/duplicate quiet/max freezes; one worker chain serializes builds. |
| T-03-12 | Denial of Service | `src/hashtree/writer.ts` | high | mitigate | Streaming fixed-size chunks, bounded link arrays, durable iteration, aggregate inventory ceilings, and cancellation avoid whole-file/tree memory use. |
| T-03-13 | Tampering | canonical manifest encoder | high | mitigate | Pinned vectors, bytewise ordering, shortest integer/binary encoding, decoder round-trip, boundary hashes, and build-twice equality. |
| T-03-14 | Spoofing | pending candidate state | high | mitigate | Separate schema/types/APIs; pending never updates committed overlay and no Phase 3 network/signing path exists. |
| T-03-15 | Repudiation | interrupted build recovery | medium | mitigate | Durable batch states and deterministic restart transitions preserve which frozen generation/root inventory was attempted. |
</threat_model>

<verification>
Run protocol and batch integration tests, then `deno task verify`. Check that the three durable boundaries are separate in schema/API and that all eight phase requirement IDs appear across plans 03-01 through 03-03.
</verification>

<success_criteria>
The daemon deterministically freezes one dependency-closed batch at five quiet or sixty sustained seconds, builds a canonical persistent COW plaintext tree, and stores a restart-safe pending candidate without replication, signing, publication, or read-root promotion.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Authorized staging becomes safely readable without incomplete exposure or premature publication | 03-01..03 | COVERED | Three explicit durable boundaries are traced end to end. |
| REQ | WRIT-01 | One exact signer-owned writable identity | 03-01 | COVERED | Derived pubkey exact-match gate. |
| REQ | WRIT-02 | Applesauce NIP-46 or protected local signer | 03-01 | COVERED | One narrow capability and protected sources. |
| REQ | WRIT-03 | PUT disabled without all prerequisites | 03-01 | COVERED | Atomic fail-closed readiness. |
| REQ | WRIT-04 | Streamed durable idempotent PUT | 03-01 | COVERED | Quota reservation, hashing, promotion, retry/conflict. |
| REQ | WRIT-05 | Dependency-closed eligibility | 03-02 | COVERED | Durable reverse fixed point and cycles. |
| REQ | WRIT-06 | Signer-first complete-only overlay | 03-02 | COVERED | Immutable generations and pinned provenance. |
| REQ | PUBL-01 | 5s quiet / 60s sustained serialized freeze | 03-03 | COVERED | Tokenized dual timer and one worker. |
| REQ | PUBL-02 | Deterministic plaintext persistent COW tree | 03-03 | COVERED | Canonical writer and pending inventory. |
| RESEARCH | — | Stock Nix NAR/narinfo arrival order and identity Content-Encoding policy | 03-01 | COVERED | Either order accepted; non-identity metadata explicitly rejected with tests. |
| RESEARCH | — | Exact Applesauce 6.2.2 lifecycle and package legitimacy | 03-01 | COVERED | Exact pin; explicit approval recorded; no signEvent. |
| RESEARCH | — | Reverse dependency fixed point and immutable generations | 03-02 | COVERED | Bounded affected-set recomputation. |
| RESEARCH | — | Pinned BUD constants, canonical encoding, COW reuse, durable pending state | 03-03 | COVERED | Protocol and restart tests. |
| CONTEXT | D-01 | Three durable boundaries: staged, committed overlay, pending candidate | 03-01..03 | COVERED | Separate types/tables/APIs. |
| CONTEXT | D-02 | Applesauce signer lifecycle for NIP-46/local with exact ownership | 03-01 | COVERED | Status-only lifecycle and protected sources. |
| CONTEXT | D-03 | Streamed bounded idempotent staging | 03-01 | COVERED | Atomic reservation/promotion. |
| CONTEXT | D-04 | Dependency-closed eligibility and signer-first immutable view | 03-02 | COVERED | Fixed point and generation capture. |
| CONTEXT | D-05 | 5s quiet/60s sustained serialized batch | 03-03 | COVERED | Fake-clock tokenized scheduler. |
| CONTEXT | D-06 | Deterministic plaintext persistent COW tree and pending candidate | 03-03 | COVERED | No early publication. |
| CONTEXT | Deferred | Replication, completeness proof, signing, relay publication, retries, full publish E2E | — | EXCLUDED | Explicit Phase 4 scope. |

<output>
Create `.planning/phases/03-signer-gated-writable-cache/03-03-SUMMARY.md` when done.
</output>
