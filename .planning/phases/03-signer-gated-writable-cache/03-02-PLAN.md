---
phase: 03-signer-gated-writable-cache
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/persistence/write_repository.ts
  - src/write/eligibility.ts
  - src/write/overlay.ts
  - src/nix/http_handler.ts
  - src/nix/merged_cache.ts
  - src/runtime/daemon.ts
  - tests/integration/writable_cache_test.ts
  - tests/integration/merged_cache_test.ts
autonomous: true
requirements: [WRIT-05, WRIT-06]
must_haves:
  truths:
    - "WRIT-05: a staged object becomes eligible only when its strict narinfo, referenced NAR, and transitive declared store references resolve from the candidate plus committed lower layers; unanchored cycles remain ineligible."
    - "WRIT-06: one dependency-closed generation commits atomically as the highest-priority signer overlay; incomplete staging never appears in GET/HEAD."
    - "A request captures one immutable signer-plus-publisher snapshot, and a signer-layer narinfo pins its NAR to the same overlay generation."
  artifacts:
    - path: "src/write/eligibility.ts"
      provides: "Bounded deterministic reverse-dependency fixed-point eligibility"
      exports: ["EligibilityModel"]
    - path: "src/write/overlay.ts"
      provides: "Immutable signer overlay generations and resolver"
      exports: ["SignerOverlay", "SignerOverlaySnapshot"]
    - path: "src/persistence/write_repository.ts"
      provides: "Durable dependency graph and atomic overlay-generation membership"
  key_links:
    - from: "src/write/eligibility.ts"
      to: "src/persistence/write_repository.ts"
      via: "durable reverse edges drive bounded affected-set recomputation and one closure transaction"
      pattern: "reference|eligib"
    - from: "src/nix/http_handler.ts"
      to: "src/write/overlay.ts"
      via: "request-entry immutable snapshot prepends signer layer before publisher layers"
      pattern: "overlay"
    - from: "src/nix/merged_cache.ts"
      to: "src/write/overlay.ts"
      via: "winner provenance pins signer narinfo URL resolution to its generation"
      pattern: "generation"
---

<objective>
Turn durable staged facts into a dependency-closed, signer-first immutable read overlay per D-01 and D-04, implementing WRIT-05 and WRIT-06.

Purpose: Make complete uploads readable while preserving the strict separation between staged bytes, committed overlay state, and publisher selections.
Output: Durable reverse-dependency eligibility model, atomic overlay generations, signer-first merged resolution, and incomplete/cycle/snapshot tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-signer-gated-writable-cache/03-CONTEXT.md
@.planning/phases/03-signer-gated-writable-cache/03-RESEARCH.md
@.planning/phases/03-signer-gated-writable-cache/03-01-SUMMARY.md
@src/protocol/narinfo.ts
@src/nix/merged_cache.ts
@src/nix/http_handler.ts
@src/nostr/selection.ts

<interfaces>
Consume Plan 03-01's `WriteRepository` committed staged rows and status-only `SignerCapability`. Existing merged requests capture `MergedSelectionSnapshot` once, resolve Narinfo through `resolveMergedNarInfo`, and pin winner NAR paths with `WinnerRouteRegistry`; extend these contracts with a distinct immutable signer generation rather than mutating publisher arrays.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one complete staged object into a signer-first immutable GET path</name>
  <files>src/persistence/write_repository.ts, src/write/eligibility.ts, src/write/overlay.ts, src/nix/http_handler.ts, tests/integration/writable_cache_test.ts</files>
  <behavior>
    - A valid staged narinfo whose referenced NAR exists and whose References are already available in a committed lower layer becomes eligible and is committed in one new overlay generation.
    - GET/HEAD of that narinfo and its NAR resolves from the signer layer before every configured publisher and uses the same captured generation.
    - Missing NAR or reference leaves the narinfo staged and returns the pre-existing lower-layer result or 404, never staged bytes.
  </behavior>
  <reversibility rating="costly">The immutable overlay-generation contract becomes the read/publication handoff shared by Phase 4 and winner routing.</reversibility>
  <action>Extend the write repository with normalized staged narinfo/reference rows, reverse indexes, monotonic overlay generations, and immutable overlay entries. Implement `EligibilityModel` as a bounded deterministic work queue seeded from changed store paths: a node enters the fixed point only when its NAR is verified and every reference is either already available from the captured committed lower-layer snapshot or has entered this candidate closure. An unanchored strongly connected set never becomes eligible merely because its rows exist. Commit the entire dependency-closed membership and generation atomically before emitting a frozen `SignerOverlaySnapshot`. Implement local staged-file resolution for narinfo/NAR streams, prepend the signer snapshot to publisher layers at request capture, and pin signer NAR routes to that exact generation. Preserve existing publisher merge/signature behavior below the signer layer and never read directly from mutable staging tables during an HTTP request.</action>
  <verify>
    <automated>deno test --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/writable_cache_test.ts --filter "complete object commits to signer-first immutable overlay"</automated>
  </verify>
  <done>A real complete staged object is newly readable through the signer-first layer, while its incomplete equivalent remains invisible and every response uses one immutable generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand reactive reverse-dependency closure, cycles, restart, and concurrency safety</name>
  <files>src/persistence/write_repository.ts, src/write/eligibility.ts, src/write/overlay.ts, src/nix/http_handler.ts, src/nix/merged_cache.ts, src/runtime/daemon.ts, tests/integration/writable_cache_test.ts, tests/integration/merged_cache_test.ts</files>
  <behavior>
    - A dependency arriving after its dependent triggers bounded affected-set recomputation and atomically admits the full newly closed set without rescanning an unbounded dataset in memory.
    - Cyclic or unresolved candidates remain durable and invisible across restart; adding a valid lower-layer anchor admits only the now-closed affected set.
    - A response started on generation N remains byte-identical while generation N+1 commits, and signer content deterministically wins a path collision with a publisher.
  </behavior>
  <action>Persist reverse dependency edges and queue only transitively affected paths, enforcing configured visited-node/decoded-metadata ceilings and stable bytewise ordering. Restore the last fully committed overlay generation before reactive emission; ignore/repair partial non-current generations transactionally. Wire staging commit notifications into eligibility recomputation through RxJS control-plane signals, serialized so concurrent PUT completions cannot expose partial membership. Extend merged-cache provenance and route registry to distinguish signer generations from publisher event identities. Ensure reads open immutable file handles/leases for the captured generation so later staging cleanup or overlay commits cannot alter an in-flight body. Add restart, reverse arrival, unresolved SCC, collision priority, and concurrent snapshot tests.</action>
  <verify>
    <automated>deno test --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/writable_cache_test.ts tests/integration/merged_cache_test.ts</automated>
  </verify>
  <done>WRIT-05/WRIT-06 hold for reverse arrival, transitive references, cycles, restart, priority conflicts, and concurrent generation changes with bounded deterministic recomputation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged metadata→eligibility graph | Publisher-shaped Narinfo fields request visibility in the committed overlay. |
| eligibility transaction→reactive read snapshot | Durable closure becomes user-visible cache state. |
| mutable arrivals→captured HTTP generation | Concurrent writes must not change an in-flight response's provenance. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | `src/write/eligibility.ts` | high | mitigate | Monotone fixed point requires verified NAR plus all references from committed lower state or the admitted candidate; unresolved cycles never seed themselves. |
| T-03-07 | Elevation of Privilege | overlay commit | high | mitigate | Only the signer-owned write repository can atomically create a current immutable generation; staging rows are not resolver inputs. |
| T-03-08 | Denial of Service | reverse-dependency recomputation | medium | mitigate | Durable reverse index, affected-only queue, unique-node and decoded-metadata ceilings, deterministic deduplication. |
| T-03-09 | Tampering | HTTP snapshot/routing | high | mitigate | Capture signer generation and publisher selections together at request entry and pin NAR provenance to that immutable generation. |
| T-03-10 | Repudiation | eligibility diagnostics | low | accept | Single-user local daemon accepts coarse secret-safe reason codes; full operational audit logging is Phase 4 OPER-02. |
</threat_model>

<verification>
Run both integration files and `deno task check`; inspect tests proving no direct staged-row resolver path and no publisher snapshot mutation.
</verification>

<success_criteria>
Only dependency-closed objects enter a durable signer-first immutable generation, incomplete/cyclic objects remain invisible, and request snapshots survive concurrent overlay changes.
</success_criteria>

<output>
Create `.planning/phases/03-signer-gated-writable-cache/03-02-SUMMARY.md` when done.
</output>
