---
phase: 01-verified-nix-substitution-walking-slice
plan: 04
type: execute
wave: 4
depends_on: [01-02, 01-03]
files_modified:
  - src/protocol/narinfo.ts
  - src/nix/http_handler.ts
  - src/app.ts
  - main.ts
  - tests/protocol/narinfo_test.ts
  - tests/integration/http_cache_test.ts
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, OPER-01]
must_haves:
  truths:
    - "GET/HEAD expose only `/nix-cache-info`, strict `.narinfo`, and referenced NAR paths; unsupported methods return 405 with `Allow: GET, HEAD`."
    - "Per D-09, only authenticated absence is 404; per D-10, HEAD stops before the final blob; per D-11, each request captures selection exactly once through completion. Unavailable selection is 503, timeout 504, and integrity/policy/upstream failure 502."
    - "Per resolved D-12, every syntactically valid `Sig` line is emitted unchanged and endorsement against publication key bytes is recorded separately; stock Nix alone decides local trust."
    - "Startup orders parse-all config, SQLite restore, reactive selection, then listener bind; invalid config binds nothing."
  artifacts:
    - path: src/protocol/narinfo.ts
      provides: "strict lossless Sig parser/serializer and endorsement metadata"
    - path: src/nix/http_handler.ts
      provides: "stock Nix GET/HEAD adapter"
    - path: src/app.ts
      provides: "composition/shutdown lifecycle"
  key_links:
    - from: src/nix/http_handler.ts
      to: src/hashtree/reader.ts
      via: "one captured SelectedPublication is passed through the full request"
    - from: main.ts
      to: src/app.ts
      via: "listener binds only after validated composition succeeds"
  flagged_assumptions:
    - "READ-01 unclassified: stock Nix 2.34.7 accepts the chosen canonical nix-cache-info values and HEAD headers."
    - "READ-02 unclassified: strict route grammar covers the exact referenced NAR URL forms emitted by the selected fixture."
    - "READ-03 adjacency: a selection change immediately before or after capture affects only the next request."
    - "READ-03 empty: no selected snapshot maps to 503 without attempting tree access."
    - "READ-03 ordering: snapshot capture precedes route-specific asynchronous work."
    - "READ-03 concurrency: simultaneous old/new requests each retain their own immutable root."
    - "READ-04 adjacency: the first, last, and repeated valid Sig lines preserve exact bytes and ordering."
    - "READ-04 empty: a narinfo with zero Sig lines remains syntactically valid when other required fields are valid."
    - "READ-04 ordering: endorsement classification never mutates, filters, or reorders serialized Sig lines."
  prohibitions:
    - statement: "The gateway must never substitute its publisher-endorsement judgment for stock Nix trust policy or delete an otherwise valid signature."
      status: unverified
      flagged: true
    - statement: "A relay update during a request must never splice content from two publication roots."
      status: unverified
      flagged: true
    - statement: "Upstream failure must never be reported as a proven cache miss."
      status: unverified
      flagged: true
---

## Phase Goal

**As an** operator, **I want to** point a real Nix client at the daemon and substitute an uncached store path from one valid plaintext Nostr cache, **so that** decentralized cache reads preserve trust, integrity, freshness, and bounded resources.

<objective>
Expose the verified selected-tree path through stock Nix HTTP semantics and a safe daemon lifecycle.

Purpose: Complete the application walking skeleton from configuration and relay state to Nix-visible metadata/NAR streams.
Output: Lossless strict narinfo codec, snapshot-bound GET/HEAD handler, composition root, and protocol integration tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@NIP.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-02-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-03-SUMMARY.md
@main.ts
</context>

## Artifacts this phase produces

- `src/protocol/narinfo.ts`: `NarInfo`, `NarInfoSignature`, `Endorsement`, `parseNarInfo`, `serializeNarInfo`, `classifyEndorsements`
- `src/nix/http_handler.ts`: `createNixHttpHandler`, route/error helpers
- `src/app.ts`: `DaemonApp`, `createApp`, `startApp`
- updated `main.ts`
- `tests/protocol/narinfo_test.ts`, `tests/integration/http_cache_test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve narinfo signatures while classifying endorsement</name>
  <reversibility rating="one-way">D-12 defines the public gateway byte contract consumed by stock Nix; changing it requires coordinated protocol and requirement amendments.</reversibility>
  <read_first>NIP.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md</read_first>
  <files>src/protocol/narinfo.ts, tests/protocol/narinfo_test.ts</files>
  <behavior>
    - "Strict required/scalar/repeatable field parsing rejects malformed or ambiguous records."
    - "Every syntactically valid Sig line round-trips unchanged in original order, including undeclared and unsigned-cache signatures."
    - "Ed25519 verification against event-declared key bytes produces separate endorsement metadata and never edits serialized fields."
  </behavior>
  <action>Implement a line-oriented strict parser with lossless raw storage for repeatable `Sig` fields and typed semantic fields needed for NAR routing/hash/size checks. Parse signature syntax/canonical base64 strictly. Classify publisher endorsement by decoded Ed25519 key bytes from the selected event, without comparing names for identity and without filtering or reserializing signature text. D-12 is already approved and reconciled in commit `cc9921f`; carry its one-way rating for traceability and do not insert another decision checkpoint.</action>
  <verify><automated>deno test tests/protocol/narinfo_test.ts</automated></verify>
  <acceptance_criteria>Golden fixtures prove byte-identical ordered Sig output for endorsed, unendorsed, mixed, repeated, and unsigned records; malformed signatures fail strict parsing; endorsement metadata is independent.</acceptance_criteria>
  <done>The Nix metadata boundary exactly implements D-12 and READ-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve snapshot-bound Nix GET and HEAD routes</name>
  <read_first>NIP.md, src/protocol/narinfo.ts, src/nostr/selection.ts, src/hashtree/reader.ts, .planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md</read_first>
  <files>src/nix/http_handler.ts, tests/integration/http_cache_test.ts</files>
  <behavior>
    - "GET/HEAD nix-cache-info, narinfo, and referenced NAR paths use strict grammar and stock-compatible headers/bodies."
    - "The handler reads current selection exactly once and passes that immutable value through every await and stream."
    - "HEAD proves indexed presence without final content fetch; GET streams only fully verified content."
    - "Typed resolver errors map to 404/502/503/504/405 exactly as the phase contract defines."
  </behavior>
  <action>Create the standards-native handler. Capture `selection.current()` at entry before asynchronous resolution. Serve fixed canonical `nix-cache-info`; parse/serialize narinfo with unchanged signatures and separately record endorsement for diagnostics; resolve only the NAR path declared by the record. Use the D-09 taxonomy, D-10 HEAD behavior, request signal cancellation, correct content lengths where authenticated/known, and no whole-body conversions. A fixture that changes selection mid-request must show the first request remains on its original root.</action>
  <verify><automated>deno test --allow-net=127.0.0.1 --allow-read --allow-write tests/integration/http_cache_test.ts</automated></verify>
  <acceptance_criteria>All three route classes pass GET/HEAD tests; 404 occurs only for verified absence; concurrent root-swap tests never mix snapshots; NAR bytes remain streamed and signature text unchanged.</acceptance_criteria>
  <done>An unmodified Nix HTTP client can traverse cache metadata and one NAR using a coherent selected root.</done>
</task>

<task type="auto">
  <name>Task 3: Bind the daemon only after valid durable composition</name>
  <read_first>main.ts, src/config/config.ts, src/persistence/state_repository.ts, src/nostr/selection.ts, src/nix/http_handler.ts, .planning/phases/01-verified-nix-substitution-walking-slice/01-PATTERNS.md</read_first>
  <files>src/app.ts, main.ts, tests/integration/http_cache_test.ts</files>
  <action>Keep `main.ts` guarded by `import.meta.main` and move side-effect-free composition into `createApp`. Enforce startup order: collect/print all config errors; open/migrate repository and restore state; create relay/store selection and resolver; create handler; bind last. Return a lifecycle whose shutdown aborts requests, disposes RxJS/Applesauce resources, closes repository/temp resources, then closes listener. Add a startup test proving invalid config creates no listener, DB, DNS, or relay activity.</action>
  <verify><automated>deno test --allow-net=127.0.0.1 --allow-read --allow-write tests/integration/http_cache_test.ts --filter 'startup|shutdown'</automated></verify>
  <acceptance_criteria>Invalid config reports all errors with no side effects; valid config restores state before listening; shutdown releases listener, subscriptions, store, DB, and pending request resources.</acceptance_criteria>
  <done>The Phase 1 application runs as a safe single-user daemon with an importable test seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Nix HTTP request → resolver | Untrusted path/method controls bounded backend work. |
| authenticated narinfo → Nix response | Publisher metadata must remain strict while local Nix retains trust authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12 | Tampering | narinfo serializer | high | mitigate | Lossless Sig golden tests and separate byte-key endorsement metadata per D-12. |
| T-01-13 | Tampering | request snapshot | high | mitigate | Single selection read at handler entry and explicit snapshot parameter throughout. |
| T-01-14 | Denial of service | HTTP route | medium | mitigate | Method/path allowlist, request cancellation, shared traversal budget, streamed responses. |
| T-01-15 | Repudiation | error mapping | low | accept | Phase 1 exposes deterministic statuses and rejection diagnostics; full structured operations telemetry is OPER-02 in Phase 4. |
| T-01-SC | Tampering | npm installs | high | mitigate | Uses only exact packages approved in 01-01 and the committed lockfile. |
</threat_model>

<verification>
`deno fmt --check && deno lint && deno check main.ts src/app.ts src/nix/http_handler.ts src/protocol/narinfo.ts` plus narinfo and HTTP integration suites pass.
</verification>

<success_criteria>
The complete daemon starts safely and stock-compatible HTTP requests see one immutable verified root, exact valid signatures, and accurate failure semantics.
</success_criteria>

<output>Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-04-SUMMARY.md` when done.</output>
