---
phase: 01-verified-nix-substitution-walking-slice
plan: 09
type: execute
wave: 5
depends_on: [01-03, 01-04]
files_modified:
  - src/config/config.ts
  - src/hashtree/reader.ts
  - src/nix/http_handler.ts
  - tests/integration/hostile_blossom_test.ts
  - tests/integration/http_cache_test.ts
autonomous: true
gap_closure: true
requirements: [TREE-04, TREE-05]
must_haves:
  truths:
    - "Nested file manifests emit raw chunks in authenticated logical order regardless of nesting depth."
    - "Configured per-blob transfer, aggregate request transfer, and aggregate output ceilings are debited before or during I/O and publisher-declared sizes can only narrow a ceiling."
    - "Narinfo GET rejects an authenticated descriptor above decodedMetadataBytes and incrementally reads at most that configured count before UTF-8 parsing."
    - "NAR responses remain backpressured and snapshot-bound while cancellation disposes the active verified blob and no complete NAR is buffered in memory."
  artifacts:
    - path: src/config/config.ts
      provides: "Validated per-blob, per-request transfer, and output limits"
    - path: src/hashtree/reader.ts
      provides: "Ordered traversal and transferred/output RequestBudget debits"
      exports: [RequestBudget, PathResolver]
    - path: src/nix/http_handler.ts
      provides: "decodedMetadataBytes-bounded narinfo decoding"
  key_links:
    - from: src/config/config.ts
      to: src/hashtree/reader.ts
      via: "production budget construction supplies hard transfer/output ceilings"
      pattern: "maxTransferredBytes|maxOutputBytes"
    - from: src/nix/http_handler.ts
      to: src/hashtree/reader.ts
      via: "resolved narinfo descriptor and bounded body reader enforce decodedMetadataBytes"
      pattern: "decodedMetadataBytes"
---

<objective>
Close the Hashtree ordering and resource-amplification gaps across fetch, traversal, and narinfo serving.

Purpose: Ensure authenticated publisher sizes never become resource authority and that nested files and metadata are consumed correctly under configured bounds.
Output: Extended validated limits and request ledger, ordered lazy file traversal, bounded metadata decoder, and focused behavioral tests.

<acceptance_criteria>
- A nested file manifest with at least three chunks across parent/child manifests emits exact authenticated order and size.
- No single blob exceeds the configured per-blob ceiling, no request exceeds aggregate transferred or output ceilings, and a declared size larger than policy is rejected before network or disk growth.
- Narinfo descriptors above decodedMetadataBytes are rejected before GET content acquisition where possible; streamed content crossing the ceiling is cancelled without parse/serialization.
- NAR serving remains a stream with bounded queueing and cleanup on consumer cancellation.
</acceptance_criteria>
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-CONTEXT.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-VERIFICATION.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-03-SUMMARY.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Stream an ordered nested file under one transfer/output ledger</name>
  <files>src/config/config.ts, src/hashtree/reader.ts, tests/integration/hostile_blossom_test.ts</files>
  <read_first>src/config/config.ts, src/hashtree/reader.ts, src/blossom/blob_fetcher.ts, src/protocol/hashtree.ts, tests/integration/hostile_blossom_test.ts, NIP.md</read_first>
  <behavior>
    - Test 1: parent chunks A and D around nested child chunks B and C emit A,B,C,D exactly, with arbitrary stream chunk boundaries.
    - Test 2: a declared raw size over per-blob policy is rejected before fetch; exact ceiling succeeds and one byte over fails while streaming.
    - Test 3: aggregate transferred bytes include manifests and raw attempts; aggregate output counts bytes delivered to the caller and cannot exceed configured request ceilings.
    - Test 4: duplicate DAG references debit unique manifest traversal correctly but every actual transfer/output debit remains accounted; cancellation disposes the active spool.
  </behavior>
  <action>Add positive, compiled-ceiling `blobTransferBytes`, `requestTransferBytes`, and `requestOutputBytes` fields to `Limits` and parse them with the existing aggregate side-effect-free validation. Extend `TraversalLimits`/`RequestBudget` with explicit transfer and output ledgers and methods that reject overflow before allocation/write/enqueue. In `PathResolver.#fetch`, set maxTransferBytes to the configured per-blob remainder and treat declaredSize only as an equality requirement/narrower bound; never replace operator policy with publisher data. Debit actual transferred bytes from BlobFetcher through a narrow progress callback so retries and manifests count, and debit output as cleanupStream/fileStream enqueues bytes. Replace the current stack/unshift collection with an ordered iterative frame traversal that preserves each raw link's authenticated position across nested file manifests without recursive call-stack growth. Validate expected aggregate file size against safe integers and the remaining output budget before beginning raw fetches; retain lazy one-chunk-at-a-time streaming and disposal.</action>
  <verify>
    <automated>deno test --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/hostile_blossom_test.ts --filter "ordered|transfer budget|output budget"</automated>
  </verify>
  <done>Nested multi-chunk files stream in exact order, and configured policy—not authenticated size claims—bounds each blob, total transfer, and delivered output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decode narinfo within decodedMetadataBytes</name>
  <files>src/nix/http_handler.ts, tests/integration/http_cache_test.ts</files>
  <read_first>src/nix/http_handler.ts, src/protocol/narinfo.ts, src/hashtree/reader.ts, src/config/config.ts, tests/integration/http_cache_test.ts, NIP.md</read_first>
  <behavior>
    - Test 1: a narinfo authenticated descriptor larger than decodedMetadataBytes returns the mapped gateway failure without opening the final body.
    - Test 2: an inaccurate or adversarial stream crossing decodedMetadataBytes is cancelled and never passed to parseNarInfo.
    - Test 3: exact-limit UTF-8 metadata parses and serializes losslessly, including all syntactically valid Sig lines per D-12.
    - Test 4: NAR paths remain streamed and do not use the bounded metadata materialization helper.
  </behavior>
  <action>Require `decodedMetadataBytes` in `NixHandlerDependencies` (production composition must supply ValidatedConfig.limits.decodedMetadataBytes). For narinfo GET, compare `ResolvedPath.size` to the ceiling before consuming body, then use a project-owned bounded stream reader that accumulates only up to the ceiling, cancels on overflow/error, performs fatal UTF-8 decode, and hands the resulting text to existing strict parseNarInfo/classifyEndorsements/serializeNarInfo. Keep every syntactically valid Sig unchanged per D-12 and NIP.md; do not apply the rejected review finding CR-08. Leave NAR responses as direct backpressured streams and retain request snapshot/error mapping semantics.</action>
  <verify>
    <automated>deno test --allow-read --allow-write tests/integration/http_cache_test.ts --filter "metadata bound|GET/HEAD"</automated>
  </verify>
  <done>Narinfo memory use is bounded by decodedMetadataBytes with cancellation on overflow, while valid metadata/signatures and streamed NAR behavior remain unchanged.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/config/config.ts`: `blobTransferBytes`, `requestTransferBytes`, `requestOutputBytes` validated limit fields
- `src/hashtree/reader.ts`: request transfer/output debit methods and ordered iterative file frames
- `src/nix/http_handler.ts`: bounded narinfo stream reader and required `decodedMetadataBytes` dependency
- `tests/integration/hostile_blossom_test.ts`: nested ordering and per-blob/per-request budget regressions
- `tests/integration/http_cache_test.ts`: descriptor and streamed narinfo metadata-bound regressions

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated manifest to resource policy | Signed sizes/order are data, never authority to raise operator ceilings |
| verified spool to response | Backpressured bytes cross aggregate output accounting before enqueue |
| narinfo stream to parser | Metadata crosses decodedMetadataBytes and fatal UTF-8 boundary before structured use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09-01 | Tampering | PathResolver file ordering | high | mitigate | Ordered explicit frames plus nested multi-chunk exact-byte test |
| T-01-09-02 | Denial of service | BlobFetcher/RequestBudget | high | mitigate | Operator per-blob and aggregate transfer/output ceilings debit actual bytes; publisher size cannot raise them |
| T-01-09-03 | Denial of service | narinfo handler | high | mitigate | Descriptor precheck and bounded cancelling reader using decodedMetadataBytes |
| T-01-09-04 | Information disclosure | temporary verified blobs | medium | mitigate | Preserve owner-only spools and deterministic disposal on completion/cancellation |
| T-01-09-SC | Tampering | dependency graph | high | mitigate | No install task; retain audited pinned dependencies and lock verification |
</threat_model>

<verification>
Run `deno task fmt`, `deno task lint`, `deno task check`, the complete hostile Blossom suite, and the complete HTTP cache integration suite.
</verification>

<success_criteria>
- Nested files preserve authenticated byte order.
- Per-blob, per-request transfer, and output usage cannot exceed configured limits.
- Narinfo parsing never materializes more than decodedMetadataBytes.
- NAR delivery stays streamed, backpressured, snapshot-consistent, and cancellation-safe.
</success_criteria>

<output>
Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-09-SUMMARY.md` when done.
</output>
