---
phase: 01-verified-nix-substitution-walking-slice
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - deno.json
  - tests/fixtures/nix/README.md
  - tests/e2e/nix_substitution_test.ts
  - README.md
autonomous: true
requirements: [READ-07]
must_haves:
  truths:
    - "A real pinned Nix 2.34.7 client, with only the daemon substituter and fixture trusted key, substitutes an absent store path and verifies its narinfo signatures and NAR hashes."
    - "The fixture proves relay selection, SQLite persistence, Blossom retrieval, Hashtree traversal, narinfo preservation, and streamed NAR serving are the path that satisfied Nix."
    - "Repeated and concurrent substitutions return identical verified results without another substituter or local preexisting object masking failure."
  artifacts:
    - path: tests/e2e/nix_substitution_test.ts
      provides: "stock-Nix end-to-end acceptance gate"
    - path: tests/fixtures/nix/README.md
      provides: "fixture provenance and regeneration contract"
    - path: README.md
      provides: "local full-stack run and explicit quarantine-release usage"
  key_links:
    - from: tests/e2e/nix_substitution_test.ts
      to: main.ts
      via: "spawned daemon is the sole configured HTTP substituter"
    - from: nix
      to: "fixture output path"
      via: "isolated store proves absence before and validity after substitution"
  flagged_assumptions:
    - "READ-07 idempotency: repeating the same substitution is assumed to reuse immutable verified state without changing observable bytes."
    - "READ-07 concurrency: simultaneous Nix requests are assumed to remain bounded and snapshot-consistent."
  prohibitions:
    - statement: "The final acceptance test must never pass because the target already exists locally or an unlisted substituter supplies it."
      status: unverified
      flagged: true
    - statement: "A successful daemon HTTP transfer must never be reported as phase success unless stock Nix independently accepts the signature and hashes."
      status: unverified
      flagged: true
---

## Phase Goal

**As an** operator, **I want to** point a real Nix client at the daemon and substitute an uncached store path from one valid plaintext Nostr cache, **so that** decentralized cache reads preserve trust, integrity, freshness, and bounded resources.

<objective>
Close the walking slice with the real stock Nix client and document its reproducible local operation.

Purpose: Prove protocol compatibility and prevent in-process HTTP tests from masquerading as successful substitution.
Output: Pinned signed fixture provenance, isolated Nix E2E, full verification tasks, and operator runbook.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@NIP.md
@.planning/ROADMAP.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md
@.planning/phases/01-verified-nix-substitution-walking-slice/SKELETON.md
@.planning/phases/01-verified-nix-substitution-walking-slice/01-04-SUMMARY.md
@deno.json
</context>

## Artifacts this phase produces

- `tests/fixtures/nix/README.md`
- `tests/e2e/nix_substitution_test.ts`: isolated store/daemon/relay/Blossom orchestration helpers
- `deno.json` tasks `verify`, `test:integration`, `test:nix-e2e`
- `README.md` Phase 1 configuration, run, Nix client, and quarantine-release commands

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make stock Nix substitute the walking-slice fixture</name>
  <precondition>`nix --version` reports 2.34.7 and the execution environment permits isolated temporary stores, loopback listeners, and child processes.</precondition>
  <read_first>NIP.md, .planning/phases/01-verified-nix-substitution-walking-slice/01-RESEARCH.md, .planning/phases/01-verified-nix-substitution-walking-slice/SKELETON.md, main.ts, src/app.ts, src/nix/http_handler.ts, deno.json</read_first>
  <files>tests/fixtures/nix/README.md, tests/e2e/nix_substitution_test.ts, deno.json, README.md</files>
  <behavior>
    - "The target store path is proven absent in a fresh isolated destination store before daemon startup."
    - "Only the daemon substituter and exact fixture trusted-public-key are configured; fallback substituters and local source-store visibility are excluded."
    - "Nix 2.34.7 fetches nix-cache-info, narinfo, and NAR, accepts signature/hash checks, and the destination object verifies."
    - "Repeated and concurrent substitutions remain byte-identical, bounded, and successful."
  </behavior>
  <action>Create or pin a small signed Nix fixture with documented generation inputs, public key, event, BUD-16/17/18 manifests, and Blossom blobs; never commit a private signing key. Build an E2E harness with separate source/destination stores, loopback relay/Blossom fixtures, a temporary SQLite/config root, and the actual daemon process. Assert absence before substitution, invoke the real Nix 2.34.7 CLI with no fallback substituters, verify the realized path and `nix-store --verify-path`, then exercise restart persistence, repeat, and bounded concurrency. Add exact permission-scoped Deno tasks and README commands for configuration, daemon start, Nix client use, and explicit quarantine release.</action>
  <verify><automated>deno task test:nix-e2e</automated></verify>
  <acceptance_criteria>The test fails if the target is preinstalled, any fallback substituter is enabled, the daemon is bypassed, a Sig is removed/changed, any content hash changes, or Nix rejects the realized path; otherwise pinned Nix 2.34.7 succeeds after restart and under repeat/concurrent requests.</acceptance_criteria>
  <done>READ-07 and the phase goal are proven by the unmodified Nix client rather than a protocol simulation.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete phase verification matrix</name>
  <read_first>deno.json, README.md, tests/e2e/nix_substitution_test.ts, .planning/phases/01-verified-nix-substitution-walking-slice/COVERAGE.md</read_first>
  <files>deno.json, README.md</files>
  <action>Define `deno task verify` to run formatting, lint, type checking, unit/protocol suites, permission-scoped integration suites, and the pinned Nix E2E in an explicit order. Run it from a clean process environment. Check COVERAGE.md decisions against exercised routes/fixtures and document commands and expected ports/paths without broad runtime permissions.</action>
  <verify><automated>deno task verify</automated></verify>
  <acceptance_criteria>The single verification command covers every Phase 1 suite and exits zero; README instructions reproduce the same stock-Nix flow with narrow permissions and no undocumented external dependency.</acceptance_criteria>
  <done>The walking skeleton has one reproducible, comprehensive acceptance command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test harness → Nix process/store | Ambient Nix configuration or preexisting paths can produce a false-positive acceptance. |
| committed fixture → daemon | Fixture provenance must not contain signing secrets or drift from recorded protocol revisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16 | Spoofing | E2E substituter | high | mitigate | Empty isolated destination, sole explicit daemon substituter, access-log assertions, and absence proof. |
| T-01-17 | Tampering | fixture | high | mitigate | Documented hashes/revisions, public verification key only, Nix signature and store-path verification. |
| T-01-18 | Information disclosure | fixture secrets | high | mitigate | Generate signing material ephemerally; repository scan and fixture contract forbid private keys. |
| T-01-SC | Tampering | npm installs | high | mitigate | Verification uses the approved exact lockfile and performs no implicit install/update. |
</threat_model>

<verification>
`deno task verify` passes with Nix 2.34.7, including hostile transport, restart state, bounded streaming, exact Sig preservation, and the isolated stock-client substitution.
</verification>

<success_criteria>
An operator can follow the README and an unmodified pinned Nix client retrieves and independently verifies an uncached store object exclusively through the daemon.
</success_criteria>

<output>Create `.planning/phases/01-verified-nix-substitution-walking-slice/01-05-SUMMARY.md` when done.</output>
