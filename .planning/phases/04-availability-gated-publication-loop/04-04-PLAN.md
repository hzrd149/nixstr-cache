---
phase: 04-availability-gated-publication-loop
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - tests/fixtures/publication.ts
  - tests/integration/blossom_publication_test.ts
  - tests/integration/relay_publication_test.ts
  - tests/e2e/nix_publication_roundtrip_test.ts
  - deno.json
autonomous: true
requirements: [PUBL-03, PUBL-04, PUBL-05, PUBL-06, PUBL-07, OPER-02, OPER-03, OPER-04]
must_haves:
  truths:
    - "Hostile Blossom and relay fixtures prove every availability, acknowledgement, repair, streaming, and redaction barrier."
    - "A real Nix CLI uploads a fresh store object through the production daemon, waits for its actual signed promoted root, deletes/isolates the source, and substitutes solely from that root."
    - "The unchanged full repository verification matrix passes under explicit narrow permissions."
  artifacts:
    - path: "tests/fixtures/publication.ts"
      provides: "Reusable hostile Blossom and minimal NIP-01 relay controls"
    - path: "tests/integration/blossom_publication_test.ts"
      provides: "Streaming upload and complete-replica matrix"
    - path: "tests/integration/relay_publication_test.ts"
      provides: "Acknowledgement, exact-event retry, and local relay matrix"
    - path: "tests/e2e/nix_publication_roundtrip_test.ts"
      provides: "Stock Nix upload-to-publication-to-substitution proof"
  key_links:
    - from: "tests/e2e/nix_publication_roundtrip_test.ts"
      to: "main.ts"
      via: "child production daemon and real nix copy commands"
      pattern: "nix.*copy|main\.ts"
    - from: "tests/e2e/nix_publication_roundtrip_test.ts"
      to: "src/nostr/selection.ts"
      via: "waits for the newly signed event id/root rather than a prebuilt read fixture"
      pattern: "event.*id|htree|publication"
---

<objective>
Close Phase 4 with the hostile protocol matrix and real stock-Nix write/publication/read round trip.

Purpose: Demonstrate that the complete production lifecycle—not a preconstructed fixture—satisfies the v1 availability and interoperability contract.
Output: Publication fixtures, focused hostile suites, a real Nix round-trip E2E, and final verification tasks.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-availability-gated-publication-loop/04-CONTEXT.md
@.planning/phases/04-availability-gated-publication-loop/04-RESEARCH.md
@.planning/phases/04-availability-gated-publication-loop/04-01-SUMMARY.md
@.planning/phases/04-availability-gated-publication-loop/04-02-SUMMARY.md
@.planning/phases/04-availability-gated-publication-loop/04-03-SUMMARY.md
@tests/e2e/nix_substitution_test.ts
@tests/fixtures/nostr_connect.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove production upload to newly published root with stock Nix</name>
  <files>tests/fixtures/publication.ts, tests/e2e/nix_publication_roundtrip_test.ts, deno.json</files>
  <behavior>
    - A fresh isolated source store creates and signs one object, and real `nix copy --to http://daemon` exercises production PUT routes.
    - The test waits for the exact new signed event id and htree root emitted after complete replica plus true configured-relay acknowledgement, then denies access to the source store/fixture bytes.
    - A fresh isolated destination confirms absence, substitutes with daemon as sole substituter and fallback disabled, and verifies restored path content.
    - No reusable private Nix or Nostr key is committed; keys and signer session files are generated in owner-only temp paths.
  </behavior>
  <action>Build reusable in-process Blossom and minimal NIP-01 relay controls that record public hashes/event ids and discard credential headers from failure output. Add a production-child-daemon E2E using loopback ports, explicit environment, owner-only temp databases/staging/spool/key files, pinned available `nix` and `nix-store`, and isolated local-root stores. Ensure the acceptance synchronization observes the newly generated signed event/root and promotion rather than merely seeing PUT success or reusing Phase 1 fixture data. Add the test to `test:nix-e2e` while retaining the existing substitution test.</action>
  <verify>
    <automated>deno task test:nix-e2e</automated>
  </verify>
  <done>Stock Nix uploads, triggers the real gated publication, loses all source access, and substitutes the object back solely through the newly published root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete hostile publication and bounded-stream matrix</name>
  <files>tests/fixtures/publication.ts, tests/integration/blossom_publication_test.ts, tests/integration/relay_publication_test.ts, deno.json</files>
  <behavior>
    - Blossom coverage includes slow consumers, bounded concurrent readers, truncation, size/hash/descriptor mismatch, false possession, ambiguous HEAD verified-GET fallback, server timeouts, split partial replicas, and eventual repair.
    - Relay coverage includes OK true/false, duplicate/foreign/absent responses, timeout, local relay included/excluded counting, observed-event forwarding, exact event reuse, and restart admission replay.
    - Fixtures prove upload memory/concurrency bounds with backpressure and never expose captured authorization in diagnostics or assertion dumps.
  </behavior>
  <action>Factor hostile fixture controls so tests can stop after each durable transition, throttle reads, mutate descriptors/bodies, and choose per-relay response frames. Add focused integration files matching the research validation matrix. Assert concurrency numerically from fixture reader counts, event identity byte-for-byte across restart, one-server completeness by server-keyed proofs, and secret redaction. Keep network permission loopback-only and filesystem permission limited to repository/temp paths.</action>
  <verify>
    <automated>deno test --allow-env --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/blossom_publication_test.ts tests/integration/relay_publication_test.ts</automated>
  </verify>
  <done>The complete hostile transport, streaming, relay, local-cache, retry, and restart matrix proves the production barriers and resource ceilings.</done>
</task>

<task type="auto">
  <name>Task 3: Run the unchanged phase acceptance matrix</name>
  <files>deno.json</files>
  <action>Ensure task definitions compose—not replace—the existing format, lint, type, protocol, integration, signer, publication, health/redaction, and real-Nix suites. Run the full repository verification command unchanged, then run the real-Nix lane explicitly. Fix only failures caused by Phase 4 implementation and retain narrow explicit permissions.</action>
  <verify>
    <automated>deno task verify && deno task test:nix-e2e</automated>
  </verify>
  <done>Formatting, lint, typechecking, protocol, hostile integration, signer, publication, health/redaction, and both real-Nix workflows all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> production daemon | Hostile HTTP/WebSocket peers exercise real production network and persistence seams. |
| production daemon -> Nix CLI | Stock Nix independently validates the HTTP cache, Narinfo signatures, and NAR hashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-01 | Tampering | E2E fixture isolation | high | mitigate | Use fresh isolated stores, disable source access before substitution, and wait for the exact newly signed root/event. |
| T-04-04-02 | Information disclosure | Fixture captures | high | mitigate | Generate ephemeral owner-only keys and discard/redact authorization headers and secret session material. |
| T-04-04-03 | Denial of service | Hostile streams | medium | mitigate | Use loopback fixtures, explicit deadlines, slow-consumer controls, and measured concurrency ceilings. |
| T-04-04-04 | Spoofing | Minimal relay fixture | medium | mitigate | Emit exact NIP-01 frames correlated by event id and test foreign/duplicate responses as non-acknowledgements. |
| T-04-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs; tests use installed Deno, Nix, and locked dependencies. |
</threat_model>

<verification>
`deno task verify && deno task test:nix-e2e`
</verification>

<success_criteria>
- Real Nix completes upload -> gated publication -> source deletion/isolation -> sole-substituter restore.
- Hostile fixtures prove same-server completeness, exact relay acknowledgement, deterministic restart, repair, backpressure, and redaction.
- The full unchanged repository matrix passes.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | - | Publish staged objects only after a complete reachable replica, with observable recoverable failures | 04-01..04 | COVERED |
| REQ | PUBL-03 | Stream inventory and prove one advertised server complete before signing | 04-01, 04-04 | COVERED |
| REQ | PUBL-04 | Correct signed event, relay barrier, normal reactive promotion | 04-01, 04-02, 04-04 | COVERED |
| REQ | PUBL-05 | Durable asynchronous incomplete-replica repair | 04-02, 04-04 | COVERED |
| REQ | PUBL-06 | Optional local relay read/write-through cache | 04-02, 04-04 | COVERED |
| REQ | PUBL-07 | Real Nix upload/publication/delete/substitute | 04-04 | COVERED |
| REQ | OPER-02 | Typed secret-safe structured diagnostics | 04-03, 04-04 | COVERED |
| REQ | OPER-03 | Separate pure process/read/write health | 04-03 | COVERED |
| REQ | OPER-04 | Protocol, hostile, bounded-stream, local integration, and real-Nix tests | 04-01..04 | COVERED |
| RESEARCH | - | Durable monotone saga with candidate/inventory copy | 04-01 | COVERED |
| RESEARCH | - | Same-server proof matrix and bounded upload/proof transport | 04-01, 04-04 | COVERED |
| RESEARCH | - | Persist/sign once, exact relay retry, normal admission replay | 04-01, 04-02 | COVERED |
| RESEARCH | - | Deterministic nearest-due replica/relay repair | 04-02 | COVERED |
| RESEARCH | - | Explicit ordered nixSigKeys and 30-day NIP-40 refresh policy | 04-01, 04-02 | COVERED |
| RESEARCH | - | Typed diagnostics and pure three-axis health | 04-03 | COVERED |
| CONTEXT | - | All Availability Barrier and Replication decisions | 04-01, 04-04 | COVERED |
| CONTEXT | - | All Signing, Relay Publication, and Promotion decisions | 04-01, 04-02 | COVERED |
| CONTEXT | - | All Retry and Local Event Cache decisions | 04-02, 04-04 | COVERED |
| CONTEXT | - | All Observability and Health decisions | 04-03 | COVERED |
| CONTEXT | - | All End-to-End Acceptance decisions | 04-04 | COVERED |
| CONTEXT | deferred | Multi-user, distributed gateway, BUD-15, hardware-backed keys, privacy claims | excluded | EXCLUDED |

<output>
Create `.planning/phases/04-availability-gated-publication-loop/04-04-SUMMARY.md` when done.
</output>
