---
phase: 04-availability-gated-publication-loop
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/operations/diagnostics.ts
  - src/operations/health.ts
  - src/nix/http_handler.ts
  - src/runtime/daemon.ts
  - tests/integration/health_diagnostics_test.ts
autonomous: true
requirements: [OPER-02, OPER-03, OPER-04]
must_haves:
  truths:
    - "Every required operational event is emitted as one typed JSON object containing only allow-listed secret-safe fields."
    - "GET /health independently reports process, read, and write status with stable reason codes."
    - "Health reads current in-memory/durable state only and performs no mutation, signing, timer advancement, or network I/O."
  artifacts:
    - path: "src/operations/diagnostics.ts"
      provides: "Closed diagnostic union and allow-list JSON sink"
    - path: "src/operations/health.ts"
      provides: "Pure process/read/write health snapshot"
    - path: "src/nix/http_handler.ts"
      provides: "Machine-readable /health route before stock Nix routing"
    - path: "tests/integration/health_diagnostics_test.ts"
      provides: "State matrix, purity tripwires, and redaction corpus"
  key_links:
    - from: "src/runtime/daemon.ts"
      to: "src/operations/diagnostics.ts"
      via: "selection, conflicts, upstream, signer, batch, replica, relay, and promotion callbacks share one sink"
      pattern: "diagnostic|emit"
    - from: "src/nix/http_handler.ts"
      to: "src/operations/health.ts"
      via: "GET/HEAD /health serializes one captured pure snapshot"
      pattern: "health.*current|snapshot"
---

<objective>
Provide the phase's typed secret-safe diagnostic surface and pure three-axis health endpoint.

Purpose: Let an operator distinguish a healthy read daemon with blocked publication from process/read failure without health probes causing more work.
Output: Diagnostic and health modules, production wiring, HTTP route, and adversarial tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-availability-gated-publication-loop/04-CONTEXT.md
@.planning/phases/04-availability-gated-publication-loop/04-RESEARCH.md
@.planning/phases/04-availability-gated-publication-loop/04-02-SUMMARY.md
@src/nix/merged_cache.ts
@src/nix/http_handler.ts
@src/runtime/daemon.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a blocked publication into secret-safe diagnostics and health</name>
  <files>src/operations/diagnostics.ts, src/operations/health.ts, src/nix/http_handler.ts, src/runtime/daemon.ts, tests/integration/health_diagnostics_test.ts</files>
  <behavior>
    - A failed replica emits one JSON diagnostic with type, timestamp, stable code, public identity/hash/credential-free endpoint, attempt/count/duration fields, and no recursively serialized cause.
    - The same state yields process=ok, read=ok, write=blocked with a stable no-complete-replica or repair reason as appropriate.
    - GET /health returns JSON; HEAD returns the same content length without a body; unsupported methods retain stock handler semantics.
    - Repeated health requests cause zero repository writes, relay/Blossom requests, signer calls, retries, or state transitions.
  </behavior>
  <action>Start with an integration tracer that injects a credential-bearing URL, authorization-shaped error, bunker secret, key material, and large body marker into a failed publication dependency. Create a closed `OperationalDiagnostic` discriminated union and one serializer that copies only fields explicitly defined for event rejection, merge conflict, upstream failure, signer state, batch state, replica attempt/completion, relay acknowledgement, and promotion. Create a pure `HealthSnapshotProvider` returning independent process/read/write status plus frozen stable reasons from repository health, current selection/overlay, signer ownership/readiness, destination/relay configuration, and coordinator state. Route `/health` before Nix cache paths without changing PUT or substituter behavior.</action>
  <verify>
    <automated>deno test --allow-env --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/health_diagnostics_test.ts --filter "blocked publication is observable without side effects or secrets"</automated>
  </verify>
  <done>One publication failure is diagnosable through typed JSON and pure three-axis health while process/read availability remain correctly independent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover the complete health matrix and diagnostic taxonomy</name>
  <files>src/operations/diagnostics.ts, src/operations/health.ts, src/runtime/daemon.ts, tests/integration/health_diagnostics_test.ts</files>
  <behavior>
    - Diagnostics cover event rejection, merge conflict, upstream failure, signer transition, batch transition, replication, relay acknowledgement, and promotion through the same sink.
    - Process fatal/repository failure, read available/unavailable, and write disabled/blocked/ready/repairing combinations produce deterministic independent status and reason arrays.
    - Redaction corpus never emits private key bytes, nbunk secrets, Authorization/cookie values, full NAR/Narinfo content, URL userinfo/query/fragment credentials, raw Error stack/cause, or arbitrary object properties.
    - Diagnostic sink failure cannot weaken publication barriers or crash healthy reads.
  </behavior>
  <action>Replace existing ad hoc console calls at the production composition boundary with typed diagnostic constructors while retaining only safe stable codes from lower layers. Add table-driven health state tests and a hostile redaction corpus that inspects serialized JSON recursively. Add mutation/network/signing tripwires around health calculation. Keep process/read/write meaning separate: pending or failed publication blocks write but is not by itself process or read failure; a disabled signer reports write disabled rather than process failure.</action>
  <verify>
    <automated>deno test --allow-env --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/health_diagnostics_test.ts</automated>
  </verify>
  <done>All required diagnostic types and health combinations are deterministic, secret-safe, and observationally pure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostile errors/events -> logs | Arbitrary network and publisher values reach an operator-visible sink. |
| HTTP client -> health | Untrusted callers repeatedly request internal availability state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Information disclosure | Diagnostic serializer | high | mitigate | Serialize a closed union field-by-field and reject recursive error/body/header serialization. |
| T-04-03-02 | Denial of service | /health | medium | mitigate | Compute one bounded in-memory snapshot with no synchronous probes, mutations, or retry work. |
| T-04-03-03 | Spoofing | Health status | medium | mitigate | Derive axes from authoritative repository/selection/signer/coordinator state and stable reason enums. |
| T-04-03-04 | Repudiation | Publication outcomes | medium | mitigate | Include public candidate/event/endpoint identity and attempt timing without secret fields. |
| T-04-03-05 | Tampering | Diagnostic sink failure | low | accept | Logging is non-authoritative; catch sink failures while durable saga barriers remain authoritative. |
| T-04-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs. |
</threat_model>

<verification>
Run the complete health/diagnostics suite plus `deno task check`, `deno fmt --check`, and `deno lint`.
</verification>

<success_criteria>
- All required operational events use one allow-listed JSON sink.
- Health independently and correctly reports process/read/write state with stable reasons.
- Redaction and side-effect tripwires prove logs and health cannot leak secrets or trigger work.
</success_criteria>

<output>
Create `.planning/phases/04-availability-gated-publication-loop/04-03-SUMMARY.md` when done.
</output>
