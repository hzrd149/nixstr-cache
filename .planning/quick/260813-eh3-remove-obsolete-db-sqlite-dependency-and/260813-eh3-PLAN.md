---
quick_id: 260813-eh3
status: in_progress
---

# Remove obsolete `@db/sqlite` dependency

## Task 1: Remove the unused dependency

- Files: `deno.json`, `deno.lock`
- Action: Run `deno remove @db/sqlite` while retaining existing `node:sqlite` imports.
- Verify: Active dependency configuration contains no `@db/sqlite` reference and `deno info` resolves `node:sqlite`.
- Done: The obsolete JSR dependency and lock entries are absent.

## Task 2: Correct active stack documentation

- Files: `.planning/research/STACK.md`, `.planning/research/SUMMARY.md`, `AGENTS.md`
- Action: Document Deno-bundled `node:sqlite`/`DatabaseSync`, its required capabilities and lifecycle, remove the obsolete install instruction, and cite official Deno SQLite documentation.
- Verify: Active documentation contains no `@db/sqlite` reference and historical milestone artifacts remain unchanged.
- Done: Current guidance matches the implemented persistence stack.

## Task 3: Verify the cleanup

- Files: repository checks only
- Action: Run reference scans, dependency inspection, formatting, linting, type checking, protocol tests, integration tests, and the full verification task when Nix prerequisites are available.
- Verify: All available checks pass; any environment-only limitation is recorded.
- Done: Cleanup causes no application behavior or compatibility regression.
