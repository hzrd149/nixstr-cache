---
quick_id: 260812-mw9
phase: quick-security-audit-closure
plan: 01
type: quick
status: ready
wave: 1
depends_on: []
autonomous: true
requirements:
  - WRIT-04
  - PUBL-02
  - OPER-04
files_modified:
  - src/hashtree/writer.ts
  - src/persistence/write_repository.ts
  - src/write/batch_scheduler.ts
  - tests/protocol/hashtree_writer_test.ts
  - tests/integration/publication_batch_test.ts
  - tests/integration/writable_cache_test.ts
  - .planning/phases/02-deterministic-merged-read-cache/02-SECURITY.md
  - .planning/phases/03-signer-gated-writable-cache/03-SECURITY.md
must_haves:
  truths:
    - Candidate construction never copies or materializes the complete frozen route dataset in process memory.
    - Canonical chunk and fanout boundaries have pinned exact hashes and preserve copy-on-write reuse.
    - Immutable staging promotion is demonstrably create-new, same-filesystem, crash-safe, and idempotent or is replaced with a stronger primitive.
    - Phase security artifacts distinguish closed mitigations, superseded temporal findings, accepted low risks, and conservative quota tradeoffs.
  artifacts:
    - src/hashtree/writer.ts
    - src/persistence/write_repository.ts
    - tests/protocol/hashtree_writer_test.ts
    - .planning/phases/02-deterministic-merged-read-cache/02-SECURITY.md
    - .planning/phases/03-signer-gated-writable-cache/03-SECURITY.md
  key_links:
    - WriteRepository exposes frozen batch entries as bounded ordered iteration consumed directly by HashtreeWriter.
    - PublicationBatchScheduler passes durable input and cancellation into the writer without an intermediate array copy.
    - Canonical protocol bytes are pinned to exact chunk/fanout root and boundary hashes.
    - Staging temp promotion and SQLite route admission remain no-overwrite and recoverable across interruption.
---

<objective>
Close the current security-audit blockers by making Hashtree candidate construction bounded across the whole dataset boundary, pinning canonical writer outputs, proving or strengthening immutable staging promotion, and recording accurate phase-level risk dispositions.

Purpose: Make the implementation match the project's no-whole-file/no-whole-dataset memory guarantee and preserve cryptographic/copy-on-write correctness without overstating historical phase boundaries as current vulnerabilities.
Output: A durable ordered writer input contract, bounded/cancellable tree construction, exact canonical vectors, atomic-promotion evidence or replacement, and Phase 2/3 SECURITY.md reports.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@NIP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/02-deterministic-merged-read-cache/02-VERIFICATION.md
@.planning/phases/03-signer-gated-writable-cache/03-03-PLAN.md
@.planning/phases/03-signer-gated-writable-cache/03-VERIFICATION.md
@.planning/phases/04-availability-gated-publication-loop/04-VERIFICATION.md
@src/hashtree/writer.ts
@src/persistence/write_repository.ts
@src/write/batch_scheduler.ts
@tests/protocol/hashtree_writer_test.ts
@tests/integration/publication_batch_test.ts
@tests/integration/writable_cache_test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Stream one durable frozen batch through bounded canonical tree construction</name>
  <files>src/hashtree/writer.ts, src/persistence/write_repository.ts, src/write/batch_scheduler.ts, tests/protocol/hashtree_writer_test.ts, tests/integration/publication_batch_test.ts</files>
  <behavior>
    - The production scheduler passes a durable frozen batch-entry source directly to `HashtreeWriter`; it does not call `map`, spread, `Array.from`, or an equivalent full-dataset copy before building.
    - Writer input is consumed once in canonical UTF-8 route order from durable storage with explicit maximum entries, route depth/bytes, node links, inventory blobs/bytes, and cancellation checks.
    - Files remain fixed-chunk streamed with backpressure; directory/fanout construction retains only bounded frontier/run state and persists intermediate state rather than a complete in-memory route trie or dataset-sized link list.
    - Canonical output and copy-on-write blob reuse are identical for the same ordered logical files regardless of source iteration/page boundaries.
    - Cancellation or any bound violation stops iteration/building, closes handles/cursors, leaves no pending candidate, and leaves the frozen batch durably retryable.
  </behavior>
  <action>Start with failing tests using an instrumented async ordered source that rejects eager/concurrent reads, yields more routes than a small page/frontier capacity, records cancellation/closure, and compares its result to the existing small canonical build. Refactor the `BatchWriter`/`HashtreeWriter.build` input contract away from `readonly LogicalFile[]` to a single-use ordered async iterable or repository-owned cursor abstraction carrying an explicit entry ceiling and `AbortSignal`. Add a `WriteRepository` method that selects `publication_batch_entries` in canonical route order using bounded pages or a live SQLite iterator tied to the immutable batch ID; ensure it checks the expected batch identity/status and releases statements/cursors on completion or cancellation. Update `PublicationBatchScheduler` to pass that source directly and an owned abort signal that is triggered during shutdown; remove the current `batch.entries.map(...)` production copy.

Replace the full sorted array and recursive `Node` trie in `HashtreeWriter` with an external/durable ordered construction algorithm. Consume already ordered routes, validate strict monotonic uniqueness and safe components incrementally, stream each file in `FILE_CHUNK_BYTES` blocks, and use bounded stack frames per route depth plus at-most-`maxLinks` link groups. When a directory or fanout level would otherwise accumulate beyond its bound, persist canonical intermediate manifests/runs to the candidate directory and retain only the link needed by the parent; fold closed route-prefix frames bottom-up as ordering advances. If canonical directory fanout requires deferred metadata, persist a bounded run/index rather than retaining every child. Require constructor/build limits for total logical entries, route UTF-8 bytes, route depth, inventory count/bytes, and links, using existing configured ceilings at the production call site. Check cancellation before every source advance, file read, manifest encode, and durable write. Preserve content-addressed create-new persistence, deterministic inventory ordering through bounded durable iteration rather than an inventory-wide in-memory sort, and unchanged-blob reuse. Do not substitute a larger array limit for streaming, load all SQLite rows, recursively retain the dataset tree, buffer file contents, or weaken canonical ordering.</action>
  <verify>
    <automated>deno test --allow-read=.,/tmp --allow-write=/tmp tests/protocol/hashtree_writer_test.ts --filter "bounded durable iteration" &amp;&amp; deno test --allow-env --allow-net=127.0.0.1 --allow-read=.,/tmp --allow-write=/tmp tests/integration/publication_batch_test.ts --filter "streams frozen batch"</automated>
  </verify>
  <done>A production frozen batch reaches the same canonical pending candidate through bounded, cancellable, durable ordered iteration without any complete dataset copy, route trie, link collection, or inventory sort in memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin canonical boundary hashes and prove immutable staging promotion</name>
  <files>src/hashtree/writer.ts, src/persistence/write_repository.ts, tests/protocol/hashtree_writer_test.ts, tests/integration/writable_cache_test.ts</files>
  <behavior>
    - Pinned fixtures assert exact chunk hashes, file-manifest hashes, directory/fanout hashes, root hashes, and plaintext nhash at one byte below, exactly at, and one byte above the 2,097,152-byte chunk boundary.
    - Exact hashes are pinned at `maxLinks - 1`, `maxLinks`, `maxLinks + 1`, multi-level fanout, UTF-8 route ordering, and ordered-source page/run boundaries; changing canonical encoding or grouping fails visibly.
    - Rebuilding the same vectors produces byte-identical roots/inventory and zero new blobs; changing one path reuses every unaffected content-addressed blob allowed by the canonical tree shape.
    - Staging promotion never overwrites an existing digest path, never exposes a partial file, and after interruption leaves either the prior complete object or the new complete object with retry converging idempotently.
  </behavior>
  <action>Add checked-in, human-readable vector definitions derived from the pinned BUD-16/17/18 revisions, with literal expected SHA-256 and nhash values—not values generated by the implementation during the assertion. Cover chunk threshold, file-link grouping, directory-link grouping, type-3 fanout grouping including a second level, UTF-8 ordering, and durable source page/run boundaries. Assert decoded link metadata (`count`, `first`, `last`) as well as every exact boundary/root hash, canonical bytes where compact, repeat-build equality, and COW reuse. Record the upstream pinned revisions beside the fixtures and include a deliberate mutation/discrimination assertion so fixtures cannot pass by self-referential recomputation.

Assess the actual filesystem contract of `Deno.link(temp, destination)` in this repository: temp and destination are under the same owner-only staging root; hard-link creation is atomic and fails with `AlreadyExists`, while the linked inode already contains fully streamed, synced bytes. Encode that contract in a focused test seam that deterministically interrupts after temp sync, after successful link but before temp unlink, and after temp unlink but before SQLite row commit; reopen the repository, exercise orphan recovery/retry, and prove no partial/overwritten destination, no double quota charge, and idempotent convergence. Also test a pre-existing same-digest destination and an immutable conflicting route. If runtime/filesystem evidence shows hard-link publication is not equivalent-or-stronger on supported targets, replace it with a same-directory create-new atomic primitive that retains the no-overwrite property; do not fall back to overwrite-capable rename, check-then-write races, cross-filesystem copy, or deleting an existing destination. Document the chosen primitive and its durability boundary in code/API comments and the security artifact.</action>
  <verify>
    <automated>deno test --allow-read=.,/tmp --allow-write=/tmp tests/protocol/hashtree_writer_test.ts --filter "pinned canonical boundary" &amp;&amp; deno test --allow-read=.,/tmp --allow-write=/tmp tests/integration/writable_cache_test.ts --filter "atomic promotion"</automated>
  </verify>
  <done>Canonical writer drift is caught by literal boundary hashes, COW reuse remains proven, and the supported same-filesystem promotion primitive has discriminating crash/idempotency/no-overwrite evidence or has been replaced by a demonstrably stronger implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Record closed, superseded, accepted, and transferred security findings</name>
  <files>.planning/phases/02-deterministic-merged-read-cache/02-SECURITY.md, .planning/phases/03-signer-gated-writable-cache/03-SECURITY.md</files>
  <action>Create phase security reports using the project's security-audit result structure: frontmatter with phase/date/verdict; scoped threat inventory; mitigation evidence linked to exact production files and discriminating tests; residual-risk register with severity, disposition, rationale, and owner/next review condition; and verification commands/results. In Phase 2, classify the historical GET-only surface as a temporal phase constraint superseded by Phase 3's signer-owned, conjunctively gated PUT implementation—not an accepted live vulnerability—and link the production handler/readiness tests. In Phase 3, classify the historical prohibition on signing/replication/relay promotion as a temporal phase constraint superseded by Phase 4's same-server availability proof, locally verified signed event, exact configured relay OK, durable promotion, and repair flow—not an unresolved defect. Record the bounded streaming writer and immutable promotion findings as closed only after Tasks 1-2 tests pass, citing exact evidence.

Explicitly enumerate genuinely accepted low-severity risks from the phase threat registers and explain why impact/likelihood and existing controls justify acceptance; do not silently relabel higher risks as low. Record the full-per-body reservation design as an intentional conservative availability tradeoff: it can reject safe concurrent uploads earlier than actual-byte accounting would, but prevents aggregate oversubscription and does not violate integrity/confidentiality; give it an accepted disposition unless project policy assigns it elsewhere. Mark risks owned by a later phase as `transferred` only with the receiving phase, implemented control, and verification evidence. Do not claim closure from plan text alone, erase historical context, weaken NIP.md MUST/MUST NOT language, or treat v2 deferred features as v1 vulnerabilities.</action>
  <verify>
    <automated>test -f .planning/phases/02-deterministic-merged-read-cache/02-SECURITY.md &amp;&amp; test -f .planning/phases/03-signer-gated-writable-cache/03-SECURITY.md &amp;&amp; rg -n "superseded|accepted|transferred|GET-only|signer-gated PUT|availability-gated|full.*reservation|bounded|atomic" .planning/phases/02-deterministic-merged-read-cache/02-SECURITY.md .planning/phases/03-signer-gated-writable-cache/03-SECURITY.md &amp;&amp; deno task verify</automated>
  </verify>
  <done>Phase 2 and Phase 3 security reports accurately distinguish current closed controls from superseded temporal boundaries and explicit residual dispositions, and every closure claim points to passing production evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Frozen SQLite batch to Hashtree writer | Potentially unbounded operator-uploaded route metadata and files cross into canonical authenticated tree construction. |
| Staging temp inode to immutable digest namespace | Fully received bytes become durable shared content and must never overwrite or expose partial data. |
| Historical phase threat registers to release security posture | Temporal implementation constraints and residual risks must be classified without hiding current vulnerabilities or overstating superseded findings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-mw9-01 | Denial of Service | HashtreeWriter dataset ingestion | high | mitigate | Durable ordered cursor, bounded frontier/runs, explicit entry/route/depth/inventory limits, and cancellation tests eliminate full-dataset retention. |
| T-mw9-02 | Tampering | canonical manifest grouping/hashes | high | mitigate | Literal pinned boundary hashes, decoded metadata assertions, repeat-build equality, and COW reuse vectors detect canonical drift. |
| T-mw9-03 | Tampering | staging promotion | high | mitigate | Same-filesystem create-new atomic publication plus crash-window, no-overwrite, inode completeness, and idempotent reopen tests; replace primitive if evidence fails. |
| T-mw9-04 | Repudiation | security dispositions | medium | mitigate | SECURITY.md records exact evidence, temporal supersession, explicit accept/transfer rationale, ownership, and review triggers. |
| T-mw9-05 | Denial of Service | full-per-body quota reservation | low | accept | Conservative reservation may reduce concurrency but prevents oversubscription; document measurable tradeoff without weakening the aggregate ceiling. |
| T-mw9-SC | Tampering | package supply chain | low | accept | No package installation occurs; all work uses the existing pinned Deno/dependency graph. |
</threat_model>

<source_coverage_audit>

| Source | Item | Coverage |
|---|---|---|
| GOAL | Close security audit blockers without weakening streaming, integrity, availability, or NIP.md | Tasks 1-3 |
| REQ | WRIT-04 streamed durable idempotent staging | Tasks 1-2 |
| REQ | PUBL-02 bounded canonical deterministic COW Hashtree | Tasks 1-2 |
| REQ | OPER-04 hostile/integration/protocol/E2E security evidence | Tasks 1-3 |
| RESEARCH | Durable ordered iteration, per-node bounds, fixed chunks, canonical BUD-16/17/18 encoding, COW reuse | Tasks 1-2 |
| CONTEXT | Writer/build boundary currently copies full batch and builds full in-memory trie | Task 1 |
| CONTEXT | Canonical writer needs literal exact boundary hashes | Task 2 |
| CONTEXT | `Deno.link` promotion needs equivalent-or-stronger atomicity assessment and crash evidence | Task 2 |
| CONTEXT | Phase 2 GET-only superseded by Phase 3 signer-gated PUT | Task 3 |
| CONTEXT | Phase 3 no-publication boundary superseded by Phase 4 availability-gated publication | Task 3 |
| CONTEXT | Accepted low risks and conservative full-reservation quota require explicit disposition | Task 3 |

No deferred idea, dependency installation, or unrelated scope is included; every supplied audit item is covered.
</source_coverage_audit>

<verification>
Execute Tasks 1 and 2 test-first with separate RED/GREEN/refactor commits, then write Task 3 only from passing evidence and commit it atomically. Run focused protocol/integration filters after each behavior change, then `deno task verify` for format, lint, type checking, protocol, integration, and both stock-Nix E2E flows. Inspect the final diff for array/trie whole-dataset patterns at the writer boundary, overwrite-capable promotion, self-generated expected hashes, unbounded route state, missing cancellation cleanup, and security closure claims lacking exact evidence.
</verification>

<success_criteria>
- Production candidate construction consumes immutable batch rows through bounded cancellable ordered iteration and never materializes the entire file/dataset route tree or inventory in memory.
- Literal pinned vectors cover exact chunk, link, fanout, page/run, root, and nhash boundaries while preserving deterministic COW reuse.
- Same-filesystem immutable promotion has targeted crash/idempotency/no-overwrite proof, or is replaced if that proof fails.
- Phase 2 and Phase 3 SECURITY.md files document closed mitigations, temporal supersession, transferred responsibilities, accepted low risks, and the conservative reservation tradeoff accurately.
- `deno task verify` passes with no new dependency and no weakened security, streaming, canonical, or availability guarantee.
</success_criteria>

<output>
Create `.planning/quick/260812-mw9-close-security-audit-blockers-bounded-st/260812-mw9-SUMMARY.md` when done.
</output>
