---
quick_id: 260813-fd1
type: quick
status: ready
files_modified:
  - src/config/config.ts
  - tests/integration/operator_config_test.ts
  - config.example.json
  - .env.example
  - README.md
  - nix/module.nix
  - nix/example-vm.nix
  - nix/VM-EXAMPLE.md
---

<objective>
Expand read-side cache identity inputs to accept bare hex pubkeys, npub values,
canonical identities, and kind-37091 naddr values, while normalizing all inputs
to canonical identities before duplicate detection and preserving configured
priority order. Writable identities and the legacy publisher fallback remain
strict and unchanged.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Implement and test read-cache identity normalization</name>
  <files>src/config/config.ts, tests/integration/operator_config_test.ts</files>
  <action>Add a config-boundary parser using nostr-tools NIP-19 decoding. Validate decoded entity type, naddr kind/name, and canonical raw forms; ignore naddr relay hints. Normalize before duplicate detection and publisher derivation. Cover all accepted aliases, order, errors, duplicates, relay isolation, maximum count, side-effect safety, and writable-identity strictness.</action>
  <verify>deno test --allow-env --allow-read=.,/tmp --allow-write=/tmp tests/integration/operator_config_test.ts</verify>
</task>

<task type="auto">
  <name>Update operator examples and documentation</name>
  <files>config.example.json, .env.example, README.md, nix/module.nix, nix/example-vm.nix, nix/VM-EXAMPLE.md</files>
  <action>Prefer bare pubkeys or npub for default caches and naddr for named caches, while clearly retaining canonical writable identity examples.</action>
  <verify>deno fmt --check && deno lint && deno check main.ts tests/integration/operator_config_test.ts</verify>
</task>

</tasks>

<success_criteria>
- Both JSON arrays and comma-separated environment values use the same parser.
- Valid aliases normalize to canonical identities in configured order.
- Duplicate aliases and all malformed/unsupported forms fail before side effects.
- Writable identities still accept canonical raw identities only.
- Focused tests and all formatting, lint, and type checks pass.
</success_criteria>
