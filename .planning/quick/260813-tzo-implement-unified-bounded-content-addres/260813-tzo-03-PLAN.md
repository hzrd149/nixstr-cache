---
quick_id: 260813-tzo
phase: 260813-tzo-implement-unified-bounded-content-addres
plan: 03
type: execute
wave: 3
depends_on: [260813-tzo-02]
status: ready
autonomous: true
commit: true
files_modified:
  - src/persistence/write_repository.ts
  - src/hashtree/writer.ts
  - src/write/overlay.ts
  - src/write/batch_scheduler.ts
  - src/write/publication_coordinator.ts
  - src/runtime/daemon.ts
  - tests/protocol/hashtree_writer_test.ts
  - tests/integration/writable_cache_test.ts
  - tests/integration/publication_batch_test.ts
  - tests/integration/publication_recovery_test.ts
must_haves:
  truths:
    - NAR PUT streams once into canonical fixed-size chunks without a full-body staging file.
    - Route, overlay, generation, batch, candidate, and saga state owns hashes/components rather than physical paths.
    - Failed or superseded write state promptly releases final-unowned write bytes; publication recovery retains every still-owned blob.
    - A publication root is exposed only after one advertised server has the complete reachable tree.
  artifacts:
    - src/persistence/write_repository.ts
    - src/hashtree/writer.ts
    - src/write/publication_coordinator.ts
  key_links:
    - PUT upload owners atomically transfer to route/generation owners after complete validation.
    - HashtreeWriter admits canonical manifests through BlobStore and returns hash inventories.
    - Publication owner transfers commit before predecessor owners release.
---

# Plan 03: Chunked writes, overlay, Hashtree building, and publication

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stream NAR PUT into canonical store components</name>
  <files>src/persistence/write_repository.ts, src/write/overlay.ts, src/runtime/daemon.ts, tests/integration/writable_cache_test.ts</files>
  <action>Replace whole-body NAR staging with a backpressured `FILE_CHUNK_BYTES` pipeline and update daemon WriteRepository/overlay construction and handler dependencies in the same task so the new contracts compile. Reserve against request and global ceilings, incrementally hash/admit chunks under a temporary upload owner, construct canonical file-manifest levels as chunks finish, and atomically expose the route descriptor only after declared/body limits and immutable-route checks pass. Keep small narinfo metadata as one store blob. Represent routes and overlay generations as ordered hash/size/type components. Transfer owners transactionally; abort, oversize, conflict, and construction failure release upload ownership and immediately clean final-unowned write bytes. Preserve idempotent identical PUT, reference validation, writable authorization, and generation leases.</action>
  <verify><automated>deno test --allow-read=.,/tmp --allow-write=/tmp tests/integration/writable_cache_test.ts</automated></verify>
  <done>NAR uploads are chunked once while streaming, duplicate content counts once, and failure paths leave no whole NAR or orphaned write ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build canonical Hashtrees from stored route components</name>
  <files>src/hashtree/writer.ts, src/write/batch_scheduler.ts, src/runtime/daemon.ts, tests/protocol/hashtree_writer_test.ts, tests/integration/publication_batch_test.ts</files>
  <action>Refactor frozen batches and HashtreeWriter inputs to component descriptors, updating daemon writer and scheduler factories at the same commit boundary. Reuse existing chunks, admit every canonical manifest through BlobStore, and produce a leased hash inventory rather than candidate paths or a candidate-blobs directory. Preserve deterministic `FILE_CHUNK_BYTES` boundaries, logical descendant sizes, copy-on-write reuse, limits, build cancellation, and writer-run cleanup. Test N-1/N/N+1, multi-level files, repeated chunks, update builds, and failure recovery.</action>
  <verify><automated>deno test --allow-read=.,/tmp --allow-write=/tmp tests/protocol/hashtree_writer_test.ts tests/integration/publication_batch_test.ts &amp;&amp; deno task check</automated></verify>
  <done>Canonical trees build directly from the shared store with deterministic roots and no duplicate staging/candidate byte store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Transfer publication ownership and recover pending sagas</name>
  <files>src/persistence/write_repository.ts, src/write/batch_scheduler.ts, src/write/publication_coordinator.ts, src/runtime/daemon.ts, tests/integration/publication_batch_test.ts, tests/integration/publication_recovery_test.ts</files>
  <action>Replace path-bearing candidate/saga inventories with hashes and leased upload streams, and update daemon publication coordinator construction/callbacks before the task completes. Atomically transfer owners across writer-run, batch, saga, admitted generation, committed/history, and cleanup transitions before releasing predecessors. Keep replica proofs/retries visible and prohibit event publication until one advertised server has the complete reachable tree. On archive/final completion, immediately delete write-origin bytes only after the last durable owner and transient lease. Test restart at every publication stage, partial replica failure, lease cancellation, owner transfer rollback, and final cleanup.</action>
  <verify><automated>deno test --allow-read=.,/tmp --allow-write=/tmp --allow-net=127.0.0.1 tests/integration/publication_batch_test.ts tests/integration/publication_recovery_test.ts &amp;&amp; deno task check</automated></verify>
  <done>Publication remains available and resumable without byte loss, and completed/superseded write data follows exact final-owner lifetime semantics.</done>
</task>

</tasks>

<verification>Run the three focused commands, format/lint/check, and inspect that no writable runtime path creates whole-NAR staging or candidate-blobs files.</verification>
<success_criteria>Writable bytes enter once as chunks, remain owned through publication recovery, and are deleted exactly when final durable ownership and leases end.</success_criteria>
