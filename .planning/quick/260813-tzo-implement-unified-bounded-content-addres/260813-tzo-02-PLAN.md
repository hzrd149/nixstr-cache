---
quick_id: 260813-tzo
phase: 260813-tzo-implement-unified-bounded-content-addres
plan: 02
type: execute
wave: 2
depends_on: [260813-tzo-01]
status: ready
autonomous: true
commit: true
files_modified:
  - src/blossom/blob_fetcher.ts
  - src/hashtree/reader.ts
  - src/runtime/daemon.ts
  - tests/integration/blob_store_test.ts
  - tests/integration/hostile_blossom_test.ts
must_haves:
  truths:
    - Reads hit the persistent store before the network and retain verified remote misses as evictable cache.
    - Every opened blob is lease-protected and releases on EOF, cancellation, and error.
    - Remote transfer, hash, SSRF, attempt, traversal, and decoded/output bounds remain enforced with backpressure.
    - HEAD derives authenticated logical size without opening leaf chunks, including the preserved partial-NAR compatibility path.
  artifacts:
    - src/blossom/blob_fetcher.ts
    - src/hashtree/reader.ts
  key_links:
    - BlobFetcher admits verified remote streams into BlobStore and returns leased handles.
    - PathResolver concatenates ordered leased chunks while ManifestCache remains independent.
---

# Plan 02: Persistent remote read cache and resolver leases

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace temporary fetch spools with verified store admission</name>
  <files>src/blossom/blob_fetcher.ts, src/runtime/daemon.ts, tests/integration/blob_store_test.ts, tests/integration/hostile_blossom_test.ts</files>
  <action>Inject BlobStore into BlobFetcher and update the daemon's fetcher construction in the same atomic change so the repository compiles after this task. Lookup and lease the expected hash before network. On a miss retain SafeFetcher redirect/SSRF/timeouts, source attempts, declared/transfer ceilings, quarantine, and incremental hashing; reserve capacity, stream to BlobStore admission, expose only after hash verification, mark origin remote/mixed, and return a lease. Remove delete-on-dispose spool ownership. Ensure status failure, oversize, mismatch, abort, admission failure, and duplicate races release response, temp, reservation, and lease state. Test warm reuse without another request, remote retention, deterministic eviction, lease exclusion, and every cleanup path.</action>
  <verify><automated>deno test --allow-read=.,/tmp --allow-write=/tmp --allow-net=127.0.0.1 tests/integration/blob_store_test.ts tests/integration/hostile_blossom_test.ts</automated></verify>
  <done>Verified remote bytes persist as bounded evictable cache entries and no fetch terminal path leaks resources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stream resolver output through ordered transient leases</name>
  <files>src/hashtree/reader.ts, src/runtime/daemon.ts, tests/integration/hostile_blossom_test.ts</files>
  <action>Adapt manifest loading and raw-link streaming to leased BlobStore handles, and update daemon resolver construction/callback types before completing this task so `deno task check` passes at the commit boundary. Acquire before open, concatenate chunks in authenticated order with Web Stream backpressure, release each lease at EOF and release the current/pending state on cancel/error. Preserve all traversal budgets, raw hash/size checks, output ceilings, Task 1's exact legacy wire-size compatibility, and HEAD manifest-only traversal with no leaf opens. Keep ManifestCache unchanged and outside BlobStore ownership/accounting.</action>
  <verify><automated>deno test --allow-read=.,/tmp --allow-write=/tmp --allow-net=127.0.0.1 tests/integration/hostile_blossom_test.ts &amp;&amp; deno task check</automated></verify>
  <done>GET and HEAD resolve through bounded leases without eviction races, buffering, or weakening authenticated-size behavior.</done>
</task>

</tasks>

<verification>Run the focused hostile Blossom suite with read/write/net permissions, then formatting, lint, check, and `git diff --check`.</verification>
<success_criteria>Cold reads admit verified bytes, warm reads avoid the network, and cancellation/error/eviction cannot truncate or leak a live stream.</success_criteria>
