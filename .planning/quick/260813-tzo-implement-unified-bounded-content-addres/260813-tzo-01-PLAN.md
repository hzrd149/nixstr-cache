---
quick_id: 260813-tzo
phase: 260813-tzo-implement-unified-bounded-content-addres
plan: 01
type: execute
wave: 1
depends_on: []
status: ready
autonomous: true
commit: true
files_modified:
  - src/hashtree/reader.ts
  - src/hashtree/writer.ts
  - tests/integration/hostile_blossom_test.ts
  - tests/protocol/hashtree_writer_test.ts
  - src/persistence/blob_store.ts
  - src/persistence/write_repository.ts
  - src/config/config.ts
  - src/operations/diagnostics.ts
  - tests/integration/blob_store_test.ts
  - tests/integration/blob_store_migration_test.ts
  - tests/integration/operator_config_test.ts
must_haves:
  truths:
    - The bounded partial-NAR compatibility repair already present in immutable commit de3d233 is verified before storage work proceeds.
    - One persistent SHA-256 store counts each physical blob once and enforces a default 16 GiB ceiling over ready bytes plus live reservations.
    - Durable owners, transient leases, origin class, eviction state, and deletion retry state survive or reconcile correctly across restart.
    - Legacy spool, whole-body staging, candidate inventory, and pending saga state migrate compatibly and idempotently without trusting filenames.
    - The decoded manifest LRU remains separate from persistent byte accounting.
  artifacts:
    - src/persistence/blob_store.ts
    - tests/integration/blob_store_test.ts
    - tests/integration/blob_store_migration_test.ts
  key_links:
    - BlobStore catalog and WriteRepository ownership changes share one SQLite transaction boundary.
    - Startup migration commits new hash ownership before removing any recognized legacy file.
---

# Plan 01: Storage kernel, configuration, recovery, and migration

<tasks>

<task type="auto">
  <name>Task 1: Verify the existing partial-NAR compatibility prerequisite</name>
  <files>src/hashtree/reader.ts, src/hashtree/writer.ts, tests/integration/hostile_blossom_test.ts, tests/protocol/hashtree_writer_test.ts</files>
  <action>Treat immutable existing commit `de3d233` (`fix: harden NAR content resolution`) as the satisfied prerequisite; do not amend, replace, split, revert, cherry-pick, or otherwise rewrite it, and do not create a redundant prerequisite commit. Verify that the commit exists and contains changes to all four required reader/writer/regression files. Then run the focused suites to prove the committed behavior remains present: writer links distinguish manifest wire bytes from descendant plaintext bytes; bounded reader preflight accepts spec-correct sizes or only the exact old canonical wire-size fingerprint, derives authenticated logical size, avoids leaf reads for HEAD, and rejects arbitrary mismatches before HTTP framing. This task is verification-only/no-op after the checks pass; proceed directly to Tasks 2 and 3.</action>
  <verify><automated>git cat-file -e de3d233^{commit} &amp;&amp; for path in src/hashtree/reader.ts src/hashtree/writer.ts tests/integration/hostile_blossom_test.ts tests/protocol/hashtree_writer_test.ts; do git diff-tree --no-commit-id --name-only -r de3d233 -- "$path" | grep -Fx "$path" &gt;/dev/null || exit 1; done &amp;&amp; deno test --allow-read=.,/tmp --allow-write=/tmp --allow-net=127.0.0.1 tests/protocol/hashtree_writer_test.ts tests/integration/hostile_blossom_test.ts</automated></verify>
  <done>Commit de3d233 exists, contains all four required compatibility source/test changes, and the focused regressions pass; no history or worktree change is made by this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the capacity-authoritative BlobStore</name>
  <files>src/persistence/blob_store.ts, src/persistence/write_repository.ts, src/config/config.ts, src/operations/diagnostics.ts, tests/integration/blob_store_test.ts, tests/integration/operator_config_test.ts</files>
  <action>Create a daemon-owned BlobStore rooted beside the state database, with immutable `blobs/&lt;prefix&gt;/&lt;lowercase-sha256&gt;` files and catalog tables for size, write/remote/mixed origin, last access, ready/deleting state, durable owners, reservations, and tombstones. Use the WriteRepository SQLite connection or ATTACH on that connection so owner transfers and capacity metadata share `BEGIN IMMEDIATE`. Expose reserved streaming admission, verified-file admission, lookup/lease, owner acquire/transfer/release, inventory, usage/health, reconcile, and close. Default the one hard ceiling to exactly `16 * 1024 * 1024 * 1024`; charge ready physical bytes once plus live worst-case reservations, not SQLite/WAL or decoded manifest LRU. Evict only unowned/unleased remote or mixed blobs in last-access/hash order. Promote through owner-only same-filesystem temp files with incremental SHA-256, file sync, no-overwrite placement, directory sync, then catalog commit. Delete final-unowned write-origin bytes immediately or durably tombstone retry; retain final-unowned remote bytes as evictable. Add bounded diagnostics and injected-limit/concurrency/fault tests.</action>
  <verify><automated>deno test --allow-env --allow-read=.,/tmp --allow-write=/tmp tests/integration/blob_store_test.ts tests/integration/operator_config_test.ts &amp;&amp; deno task check</automated></verify>
  <done>One tested store authoritatively enforces capacity, deduplication, leases, ownership lifetimes, deterministic eviction, crash reconciliation, and a separate manifest-cache bound.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate legacy storage idempotently</name>
  <files>src/persistence/blob_store.ts, src/persistence/write_repository.ts, src/config/config.ts, src/operations/diagnostics.ts, tests/integration/blob_store_migration_test.ts</files>
  <action>Add schema/version and per-route migration markers. Rehash every legacy file. Import candidate `content_blobs` and writer/batch/saga owners first, atomically replace path references with hashes, then migrate small staged metadata as one blob and stream whole staged NARs through the canonical `FILE_CHUNK_BYTES` component representation. Commit every new owner/route swap before deleting its legacy file. Dual-read incomplete legacy rows but write only the new representation. Treat only exact `.nixstr-spool-*` names under the configured legacy spool as abandoned; quarantine/report unknown files. On startup clear stale reservations/temp names, retry tombstones, validate catalog size/existence, and safely import or quarantine hash-named orphans. Preserve writable-identity mismatch behavior and pending publication recovery. Keep old spool/staging directory fields solely as migration inputs during the compatibility window.</action>
  <verify><automated>deno test --allow-read=.,/tmp --allow-write=/tmp tests/integration/blob_store_migration_test.ts &amp;&amp; deno task check</automated></verify>
  <done>Migration can restart at every checkpoint, preserves readable/publishable live state, rejects mismatches, and never deletes unknown legacy content.</done>
</task>

</tasks>

<verification>Run all three focused commands, `deno fmt --check`, `deno lint`, `deno task check`, and `git diff --check`.</verification>
<success_criteria>The immutable compatibility prerequisite is verified without history rewrite, and the storage kernel plus legacy migration are ready for adapters without a second capacity authority.</success_criteria>
