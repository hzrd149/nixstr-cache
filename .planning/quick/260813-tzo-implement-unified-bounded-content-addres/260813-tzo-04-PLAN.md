---
quick_id: 260813-tzo
phase: 260813-tzo-implement-unified-bounded-content-addres
plan: 04
type: execute
wave: 4
depends_on: [260813-tzo-03]
status: ready
autonomous: true
commit: true
files_modified:
  - main.ts
  - src/config/config.ts
  - src/runtime/daemon.ts
  - src/blossom/source_plan.ts
  - src/blossom/cache_sink.ts
  - src/operations/diagnostics.ts
  - tests/integration/operator_config_test.ts
  - tests/integration/blob_store_migration_test.ts
  - tests/e2e/nix_publication_roundtrip_test.ts
  - tests/e2e/nix_substitution_test.ts
must_haves:
  truths:
    - Daemon startup reconciles/migrates the one store before service activation and shutdown drains users before closing it.
    - localBlossomUrl and its environment, HTTP sink, source role, docs, examples, and test surface are removed.
    - Preferred, event, and BUD-03 remote sources retain their priority and SSRF protections.
    - Cold and warm stock-Nix substitution plus writable publication succeed with byte-identical output and bounded storage.
  artifacts:
    - src/runtime/daemon.ts
    - src/config/config.ts
    - tests/e2e/nix_substitution_test.ts
    - tests/e2e/nix_publication_roundtrip_test.ts
  key_links:
    - Main/config construct one BlobStore and daemon injects it into all read/write services.
    - Direct BlobStore lookup replaces the obsolete loopback HTTP cache path.
---

# Plan 04: Runtime/config cleanup and stock-Nix verification

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire daemon lifecycle and remove localBlossomUrl</name>
  <files>main.ts, src/config/config.ts, src/runtime/daemon.ts, src/blossom/source_plan.ts, src/blossom/cache_sink.ts, src/operations/diagnostics.ts, tests/integration/operator_config_test.ts, tests/integration/blob_store_migration_test.ts</files>
  <action>Finalize the already-compiling daemon composition established by Plans 02 and 03: enforce startup order database/store open → reconcile/migrate → readers/writable identity/workers, and shutdown order stop intake/workers → drain builds/leases → close store/database. Remove `localBlossomUrl`, `NIXSTR_LOCAL_BLOSSOM`, local-cache source roles, HTTP population wiring, diagnostics, and `BlobCacheSink` (delete if unused) from main, config, runtime, tests, docs, and examples. Do not add an implicit loopback replacement: local caching is direct store lookup. Preserve preferred/event/BUD-03 source ordering, SSRF policy, writable activation gates, and legacy spool/staging fields only as migration inputs.</action>
  <verify><automated>deno test --allow-env --allow-read=.,/tmp --allow-write=/tmp --allow-net=127.0.0.1 tests/integration/operator_config_test.ts tests/integration/blob_store_migration_test.ts &amp;&amp; ! rg 'localBlossomUrl|NIXSTR_LOCAL_BLOSSOM' main.ts src tests docs examples 2&gt;/dev/null &amp;&amp; deno task check</automated></verify>
  <done>The daemon owns exactly one shared store lifecycle, obsolete local HTTP cache configuration is absent everywhere, and migration compatibility remains explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove cold/warm substitution and publication end to end</name>
  <files>tests/e2e/nix_substitution_test.ts, tests/e2e/nix_publication_roundtrip_test.ts</files>
  <action>Extend stock-Nix tests to cover an empty-store cold remote fetch, warm repeat with no second remote blob request, streamed byte-identical substitution, chunked PUT/publication, daemon restart, migrated writable state, pending publication recovery where applicable, and final shared-store usage within injected limits. Preserve unmodified Nix client semantics and the partial-NAR legacy fixture. Run E2E with explicit Nix/Deno subprocess permission.</action>
  <verify><automated>deno test --allow-env --allow-read=.,/tmp --allow-write=/tmp --allow-net=127.0.0.1 --allow-run=nix,nix-store,deno tests/e2e/nix_substitution_test.ts tests/e2e/nix_publication_roundtrip_test.ts &amp;&amp; deno task verify &amp;&amp; git diff --check</automated></verify>
  <done>Both stock-Nix flows pass cold, warm, write, publication, and restart paths with byte-identical streamed output and no second byte store.</done>
</task>

</tasks>

<verification>Run the focused config/migration test, the E2E command with `--allow-run=nix,nix-store,deno`, the repository-wide negative grep, and `deno task verify`.</verification>
<success_criteria>Runtime uses one bounded store, localBlossomUrl is gone across code and collateral, and stock Nix proves the complete decentralized read/write workflow.</success_criteria>
