---
quick_id: 260813-m0r
type: quick
status: ready
autonomous: true
commit: false
---

# Prominent durable signer mismatch warning

Add a dedicated operational diagnostic for the fail-closed condition where a
configured signer resolves successfully but does not own the identity already
bound to the writable database. Render it as a conspicuous multi-line console
warning that explains why writing is disabled, identifies the configured and
durable public owners, and tells the operator not to delete or rebind state
casually.

Emit the diagnostic before signer cleanup while preserving the existing durable
ownership invariant and generic activation failure state. Add regression
coverage for the exact startup path and for secret-safe console formatting.
Run formatting, linting, type checking, and focused integration tests without
committing in the shared dirty worktree.
