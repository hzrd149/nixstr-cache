---
quick_id: 260814-g2r
status: complete
description: Remove all legacy compatibility functionality from the pre-release application
---

# Remove legacy compatibility functionality

## Task 1: Remove legacy storage migration and spool configuration

- Delete BlobStore migration APIs, migration-only schema, startup invocation, and the obsolete global spool directory configuration.
- Remove the migration integration test and update configuration, Nix module, E2E fixtures, examples, and documentation.
- Preserve the active writable staging directory used for `.narinfo` files and writer scratch state.

## Task 2: Remove malformed pre-release Hashtree compatibility

- Require file and nested-file links to declare canonical logical sizes.
- Remove acceptance and tests for roots produced by the old wire-size writer.
- Preserve bounded preflight, streaming, integrity verification, and normal canonical tree behavior.

## Task 3: Verify and commit

- Confirm no obsolete spool/migration/preferred-Blossom compatibility surface remains.
- Run formatting, lint, type checks, protocol/integration tests, and stock-Nix E2E tests.
- Commit only task-owned changes and preserve unrelated worktree edits and `config.old.json`.
