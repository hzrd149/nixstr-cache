---
quick_id: 260814-i3y
phase: quick-260814-i3y
plan: 01
type: quick
status: ready
wave: 1
depends_on: []
autonomous: true
commit: true
files_modified:
  - src/operations/diagnostics.ts
  - tests/integration/health_diagnostics_test.ts
must_haves:
  truths:
    - Publication console progress reports only changed snapshots for each batch.
    - A fully complete publication emits one clear success line without an immediately adjacent final component progress line.
    - Progress output is driven exclusively by received diagnostics/transitions, never by an interval, timeout, polling loop, or synthetic heartbeat.
    - Successful Blossom upload lines identify what was uploaded and the sanitized destination using concise scalar fields.
    - DEBUG retains every technical publication event, and TTY/non-TTY console output remains plain-text rather than JSON.
  artifacts:
    - src/operations/diagnostics.ts
    - tests/integration/health_diagnostics_test.ts
  key_links:
    - createConsoleDiagnosticSink performs DEBUG emission before console-only snapshot deduplication and completion rendering.
---

<objective>
Make publication console progress transition-oriented: suppress unchanged snapshots and replace the redundant final component counts with the existing single fully-published success message, without reducing DEBUG visibility.

Purpose: Keep operator output concise and meaningful while preserving diagnostic detail and terminal compatibility.
Output: Focused console-sink behavior and regression tests.
</objective>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Specify changed-only publication console transitions</name>
  <files>tests/integration/health_diagnostics_test.ts</files>
  <behavior>
    - The first incomplete snapshot prints its changed Blossom and relay component states; replaying the identical snapshot prints nothing further.
    - A later incomplete snapshot prints only component lines whose rendered counters changed, not unchanged sibling components.
    - The first fully complete snapshot prints exactly one `Fully published to every configured target` line and does not print the final `Blossom replicas: ... all ok` or `Publication relays: ... all ok` component line; repeating completion prints nothing.
    - Both non-TTY and TTY/color configurations obey the same semantic line counts; TTY may retain supported decoration, non-TTY contains no carriage returns or ANSI, and neither output mode emits JSON-shaped console lines.
    - A successful replica attempt names the uploaded root/blob summary and a credential/query/fragment-free Blossom endpoint; failures retain useful safe target context.
    - No output appears merely because time advances: every asserted line is caused by a concrete emitted diagnostic.
  </behavior>
  <action>Refine the focused publication diagnostic test into deterministic transition sequences for separate batch IDs. Assert exact line deltas after each emit, including an unchanged replay, a one-component-only change, and the final all-target transition. Add a TTY/color case alongside non-TTY coverage and retain secret-safety/no-object-output assertions. Exercise successful and failed `replica_attempt` diagnostics with a root hash, blob count, and endpoint containing credentials/query/fragment; assert the console says what was uploaded and where using only safe concise scalar identifiers and a sanitized endpoint. Capture DEBUG separately using the existing debug test mechanism or a narrowly scoped console.debug capture, proving duplicate and final progress diagnostics remain technically visible even when their operator-facing console lines are suppressed. Advance the injected clock without emitting a diagnostic and assert no new line, making the absence of interval/timer-driven output explicit. Do not alter publication coordinator semantics or diagnostic payload types.</action>
  <verify>
    <automated>deno test --allow-env --allow-read=. tests/integration/health_diagnostics_test.ts</automated>
  </verify>
  <done>Tests precisely define changed-only component output, completion-only final output, DEBUG preservation, and TTY/non-TTY behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deduplicate snapshots and collapse the terminal transition</name>
  <files>src/operations/diagnostics.ts, tests/integration/health_diagnostics_test.ts</files>
  <behavior>
    - Console deduplication is scoped by publication batch and compares the complete rendered component state, including totals, success, failure, retry, and exhausted counts.
    - DEBUG publication progress is invoked for every received diagnostic before any console suppression.
    - Completion remains once per batch and supersedes component rendering for the fully successful terminal snapshot.
    - Replica-attempt console messages are emitted only by the attempt event and include bounded upload identity/count plus the sanitized endpoint.
  </behavior>
  <action>In `createConsoleDiagnosticSink`, retain compact per-batch last-rendered state for Blossom and relay components, updating and writing a component only when that component's normalized snapshot changes. Evaluate the fully-published terminal condition before component writes: on its first occurrence, record completion/current state and emit only the existing success message; on repeats, emit no operational line. Keep the sink purely event-driven—add no timer, interval, delayed refresh, polling, or heartbeat mechanism. Keep `publicationDebug(item)` ahead of this console-only policy so DEBUG records unchanged and terminal snapshots. Improve replica-attempt operational wording to identify the uploaded root with a bounded abbreviated hash, blob count when present, and `endpoint(item.endpoint)` sanitized destination; never expose credentials, query, fragment, full event bodies, or JSON objects. Preserve bounded state, current clamping, status formatting, colors, non-TTY plain output, failure/retry accounting, exception isolation, and the one-line string logger contract; do not introduce JSON serialization or object arguments. Update any broad formatter expectations affected by the intentional missing terminal component lines and clearer upload lines. Commit the test contract first, then the implementation/final green adjustments as separate atomic commits, staging only these two files and leaving untracked `config copy.json` untouched.</action>
  <verify>
    <automated>deno test --allow-env --allow-read=. tests/integration/health_diagnostics_test.ts &amp;&amp; deno fmt --check src/operations/diagnostics.ts tests/integration/health_diagnostics_test.ts &amp;&amp; deno lint src/operations/diagnostics.ts tests/integration/health_diagnostics_test.ts &amp;&amp; deno check src/operations/diagnostics.ts tests/integration/health_diagnostics_test.ts &amp;&amp; deno task verify &amp;&amp; git diff --check</automated>
  </verify>
  <done>Operational publication output contains only changed intermediate components and one terminal success line, while DEBUG and all repository checks remain intact.</done>
</task>

</tasks>

<verification>
Run the focused diagnostic test during red/green development, then formatting, linting, type checking, and the full repository verification. Review the scoped diff and `git status --short` before each atomic commit; do not stage, edit, delete, or otherwise alter `config copy.json`.
</verification>

<success_criteria>
- Unchanged publication snapshots add no operator-facing console lines.
- A changed incomplete snapshot reports only the component values that changed.
- Full completion emits one success message and no redundant final component count.
- No interval, timer, poll, or elapsed-time refresh can create progress output; real diagnostics are the sole trigger.
- Blossom upload results state the bounded root/blob summary and sanitized destination without leaking URL secrets.
- DEBUG still exposes every received progress snapshot with compact technical fields.
- TTY/color and non-TTY output remain correct, string-only, secret-safe, and non-JSON.
- Focused and full verification pass, with test and implementation work committed atomically and `config copy.json` preserved.
</success_criteria>
