---
quick_id: 260814-k1n
status: in_progress
validate: true
---

# Parallel Blossom replica publication

Run initial server replication concurrently under the existing publication concurrency ceiling. Promote after the first complete server, cancel sibling attempts into durable retry work, and retain serial blob processing within each server.

Replace publication proof downloads with bounded server acknowledgements: exact-size HEAD for existing blobs and matching hash/size JSON for successful uploads. Read-side verification remains unchanged.

## Tasks

1. Specify request sequences, evidence validation, concurrency, early promotion, cancellation, and repair behavior in integration tests.
2. Implement uploader acknowledgement validation without publication GET requests.
3. Implement bounded server workers with per-endpoint outcomes and first-complete cancellation.
4. Document the concurrency and possession-evidence semantics.
5. Run focused, full, recovery, shutdown, and stock-Nix verification.
